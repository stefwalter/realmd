/*
 * Generated by gdbus-codegen 2.32.1. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "realm-dbus-generated.h"

#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        ret = (g_value_get_double (a) == g_value_get_double (b));
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.realmd.Service
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:RealmDbusService
 * @title: RealmDbusService
 * @short_description: Generated C code for the org.freedesktop.realmd.Service D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-realmd-Service.top_of_page">org.freedesktop.realmd.Service</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.realmd.Service ---- */

static const _ExtendedGDBusPropertyInfo _realm_dbus_service_property_info_providers =
{
  {
    -1,
    "Providers",
    "a(sso)",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "providers",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _realm_dbus_service_property_info_pointers[] =
{
  &_realm_dbus_service_property_info_providers,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _realm_dbus_service_interface_info =
{
  {
    -1,
    "org.freedesktop.realmd.Service",
    NULL,
    NULL,
    (GDBusPropertyInfo **) &_realm_dbus_service_property_info_pointers,
    NULL
  },
  "service",
};


/**
 * realm_dbus_service_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-realmd-Service.top_of_page">org.freedesktop.realmd.Service</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
realm_dbus_service_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_realm_dbus_service_interface_info;
}

/**
 * realm_dbus_service_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #RealmDbusService interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
realm_dbus_service_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "providers");
  return property_id_begin - 1;
}



/**
 * RealmDbusService:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Service.top_of_page">org.freedesktop.realmd.Service</link>.
 */

/**
 * RealmDbusServiceIface:
 * @parent_iface: The parent interface.
 * @get_providers: Getter for the #RealmDbusService:providers property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Service.top_of_page">org.freedesktop.realmd.Service</link>.
 */

static void
realm_dbus_service_default_init (RealmDbusServiceIface *iface)
{
  /* GObject properties for D-Bus properties: */
  /**
   * RealmDbusService:providers:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-realmd-Service.Providers">"Providers"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("providers", "Providers", "Providers", G_VARIANT_TYPE ("a(sso)"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

typedef RealmDbusServiceIface RealmDbusServiceInterface;
G_DEFINE_INTERFACE (RealmDbusService, realm_dbus_service, G_TYPE_OBJECT);

/**
 * realm_dbus_service_get_providers: (skip)
 * @object: A #RealmDbusService.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-realmd-Service.Providers">"Providers"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_service_dup_providers() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
realm_dbus_service_get_providers (RealmDbusService *object)
{
  return REALM_DBUS_SERVICE_GET_IFACE (object)->get_providers (object);
}

/**
 * realm_dbus_service_dup_providers: (skip)
 * @object: A #RealmDbusService.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-realmd-Service.Providers">"Providers"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
realm_dbus_service_dup_providers (RealmDbusService *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "providers", &value, NULL);
  return value;
}

/**
 * realm_dbus_service_set_providers: (skip)
 * @object: A #RealmDbusService.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-realmd-Service.Providers">"Providers"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_service_set_providers (RealmDbusService *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "providers", value, NULL);
}

/* ------------------------------------------------------------------------ */

/**
 * RealmDbusServiceProxy:
 *
 * The #RealmDbusServiceProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * RealmDbusServiceProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #RealmDbusServiceProxy.
 */

struct _RealmDbusServiceProxyPrivate
{
  GData *qdata;
};

static void realm_dbus_service_proxy_iface_init (RealmDbusServiceIface *iface);

G_DEFINE_TYPE_WITH_CODE (RealmDbusServiceProxy, realm_dbus_service_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (REALM_DBUS_TYPE_SERVICE, realm_dbus_service_proxy_iface_init));

static void
realm_dbus_service_proxy_finalize (GObject *object)
{
  RealmDbusServiceProxy *proxy = REALM_DBUS_SERVICE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (realm_dbus_service_proxy_parent_class)->finalize (object);
}

static void
realm_dbus_service_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  info = _realm_dbus_service_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
realm_dbus_service_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  error = NULL;
  if (!g_dbus_proxy_call_finish (proxy, res, &error))
    {
      g_warning ("Error setting property `%s' on interface org.freedesktop.realmd.Service: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
}

static void
realm_dbus_service_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  info = _realm_dbus_service_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.freedesktop.realmd.Service", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) realm_dbus_service_proxy_set_property_cb, (gpointer) info);
  g_variant_unref (variant);
}

static void
realm_dbus_service_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_realm_dbus_service_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], REALM_DBUS_TYPE_SERVICE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, REALM_DBUS_TYPE_SERVICE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
realm_dbus_service_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  RealmDbusServiceProxy *proxy = REALM_DBUS_SERVICE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_service_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_service_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static GVariant *
realm_dbus_service_proxy_get_providers (RealmDbusService *object)
{
  RealmDbusServiceProxy *proxy = REALM_DBUS_SERVICE_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Providers");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static void
realm_dbus_service_proxy_init (RealmDbusServiceProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, REALM_DBUS_TYPE_SERVICE_PROXY, RealmDbusServiceProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), realm_dbus_service_interface_info ());
}

static void
realm_dbus_service_proxy_class_init (RealmDbusServiceProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (RealmDbusServiceProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = realm_dbus_service_proxy_finalize;
  gobject_class->get_property = realm_dbus_service_proxy_get_property;
  gobject_class->set_property = realm_dbus_service_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = realm_dbus_service_proxy_g_signal;
  proxy_class->g_properties_changed = realm_dbus_service_proxy_g_properties_changed;


  realm_dbus_service_override_properties (gobject_class, 1);
}

static void
realm_dbus_service_proxy_iface_init (RealmDbusServiceIface *iface)
{
  iface->get_providers = realm_dbus_service_proxy_get_providers;
}

/**
 * realm_dbus_service_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Service.top_of_page">org.freedesktop.realmd.Service</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_service_proxy_new_finish() to get the result of the operation.
 *
 * See realm_dbus_service_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
realm_dbus_service_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (REALM_DBUS_TYPE_SERVICE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Service", NULL);
}

/**
 * realm_dbus_service_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_service_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with realm_dbus_service_proxy_new().
 *
 * Returns: (transfer full) (type RealmDbusServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusService *
realm_dbus_service_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return REALM_DBUS_SERVICE (ret);
  else
    return NULL;
}

/**
 * realm_dbus_service_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Service.top_of_page">org.freedesktop.realmd.Service</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_service_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type RealmDbusServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusService *
realm_dbus_service_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (REALM_DBUS_TYPE_SERVICE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Service", NULL);
  if (ret != NULL)
    return REALM_DBUS_SERVICE (ret);
  else
    return NULL;
}


/**
 * realm_dbus_service_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like realm_dbus_service_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_service_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See realm_dbus_service_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
realm_dbus_service_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (REALM_DBUS_TYPE_SERVICE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Service", NULL);
}

/**
 * realm_dbus_service_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_service_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with realm_dbus_service_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type RealmDbusServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusService *
realm_dbus_service_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return REALM_DBUS_SERVICE (ret);
  else
    return NULL;
}

/**
 * realm_dbus_service_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like realm_dbus_service_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_service_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type RealmDbusServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusService *
realm_dbus_service_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (REALM_DBUS_TYPE_SERVICE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Service", NULL);
  if (ret != NULL)
    return REALM_DBUS_SERVICE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * RealmDbusServiceSkeleton:
 *
 * The #RealmDbusServiceSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * RealmDbusServiceSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #RealmDbusServiceSkeleton.
 */

struct _RealmDbusServiceSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_realm_dbus_service_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], REALM_DBUS_TYPE_SERVICE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, REALM_DBUS_TYPE_SERVICE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_realm_dbus_service_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_service_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_realm_dbus_service_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_service_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _realm_dbus_service_skeleton_vtable =
{
  _realm_dbus_service_skeleton_handle_method_call,
  _realm_dbus_service_skeleton_handle_get_property,
  _realm_dbus_service_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
realm_dbus_service_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return realm_dbus_service_interface_info ();
}

static GDBusInterfaceVTable *
realm_dbus_service_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_realm_dbus_service_skeleton_vtable;
}

static GVariant *
realm_dbus_service_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_realm_dbus_service_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _realm_dbus_service_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _realm_dbus_service_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _realm_dbus_service_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.realmd.Service", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _realm_dbus_service_emit_changed (gpointer user_data);

static void
realm_dbus_service_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _realm_dbus_service_emit_changed (skeleton);
}

static void realm_dbus_service_skeleton_iface_init (RealmDbusServiceIface *iface);
G_DEFINE_TYPE_WITH_CODE (RealmDbusServiceSkeleton, realm_dbus_service_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (REALM_DBUS_TYPE_SERVICE, realm_dbus_service_skeleton_iface_init));

static void
realm_dbus_service_skeleton_finalize (GObject *object)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (object);
  guint n;
  for (n = 0; n < 1; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (realm_dbus_service_skeleton_parent_class)->finalize (object);
}

static void
realm_dbus_service_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_realm_dbus_service_emit_changed (gpointer user_data)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *l;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.freedesktop.realmd.Service",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (l = connections; l != NULL; l = l->next)
        {
          GDBusConnection *connection = l->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_realm_dbus_service_schedule_emit_changed (RealmDbusServiceSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
realm_dbus_service_skeleton_notify (GObject      *object,
  GParamSpec *pspec)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _realm_dbus_service_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
realm_dbus_service_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _realm_dbus_service_schedule_emit_changed (skeleton, _realm_dbus_service_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
realm_dbus_service_skeleton_init (RealmDbusServiceSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, REALM_DBUS_TYPE_SERVICE_SKELETON, RealmDbusServiceSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 1);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_VARIANT);
}

static GVariant *
realm_dbus_service_skeleton_get_providers (RealmDbusService *object)
{
  RealmDbusServiceSkeleton *skeleton = REALM_DBUS_SERVICE_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
realm_dbus_service_skeleton_class_init (RealmDbusServiceSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (RealmDbusServiceSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = realm_dbus_service_skeleton_finalize;
  gobject_class->get_property = realm_dbus_service_skeleton_get_property;
  gobject_class->set_property = realm_dbus_service_skeleton_set_property;
  gobject_class->notify       = realm_dbus_service_skeleton_notify;


  realm_dbus_service_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = realm_dbus_service_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = realm_dbus_service_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = realm_dbus_service_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = realm_dbus_service_skeleton_dbus_interface_get_vtable;
}

static void
realm_dbus_service_skeleton_iface_init (RealmDbusServiceIface *iface)
{
  iface->get_providers = realm_dbus_service_skeleton_get_providers;
}

/**
 * realm_dbus_service_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Service.top_of_page">org.freedesktop.realmd.Service</link>.
 *
 * Returns: (transfer full) (type RealmDbusServiceSkeleton): The skeleton object.
 */
RealmDbusService *
realm_dbus_service_skeleton_new (void)
{
  return REALM_DBUS_SERVICE (g_object_new (REALM_DBUS_TYPE_SERVICE_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.realmd.Provider
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:RealmDbusProvider
 * @title: RealmDbusProvider
 * @short_description: Generated C code for the org.freedesktop.realmd.Provider D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-realmd-Provider.top_of_page">org.freedesktop.realmd.Provider</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.realmd.Provider ---- */

static const _ExtendedGDBusArgInfo _realm_dbus_provider_signal_info_diagnostics_ARG_data =
{
  {
    -1,
    "data",
    "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_provider_signal_info_diagnostics_ARG_pointers[] =
{
  &_realm_dbus_provider_signal_info_diagnostics_ARG_data,
  NULL
};

static const _ExtendedGDBusSignalInfo _realm_dbus_provider_signal_info_diagnostics =
{
  {
    -1,
    "Diagnostics",
    (GDBusArgInfo **) &_realm_dbus_provider_signal_info_diagnostics_ARG_pointers,
    NULL
  },
  "diagnostics"
};

static const _ExtendedGDBusSignalInfo * const _realm_dbus_provider_signal_info_pointers[] =
{
  &_realm_dbus_provider_signal_info_diagnostics,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _realm_dbus_provider_interface_info =
{
  {
    -1,
    "org.freedesktop.realmd.Provider",
    NULL,
    (GDBusSignalInfo **) &_realm_dbus_provider_signal_info_pointers,
    NULL,
    NULL
  },
  "provider",
};


/**
 * realm_dbus_provider_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-realmd-Provider.top_of_page">org.freedesktop.realmd.Provider</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
realm_dbus_provider_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_realm_dbus_provider_interface_info;
}

/**
 * realm_dbus_provider_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #RealmDbusProvider interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
realm_dbus_provider_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * RealmDbusProvider:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Provider.top_of_page">org.freedesktop.realmd.Provider</link>.
 */

/**
 * RealmDbusProviderIface:
 * @parent_iface: The parent interface.
 * @diagnostics: Handler for the #RealmDbusProvider::diagnostics signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Provider.top_of_page">org.freedesktop.realmd.Provider</link>.
 */

static void
realm_dbus_provider_default_init (RealmDbusProviderIface *iface)
{
  /* GObject signals for received D-Bus signals: */
  /**
   * RealmDbusProvider::diagnostics:
   * @object: A #RealmDbusProvider.
   * @arg_data: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-realmd-Provider.Diagnostics">"Diagnostics"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("diagnostics",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusProviderIface, diagnostics),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

}

typedef RealmDbusProviderIface RealmDbusProviderInterface;
G_DEFINE_INTERFACE (RealmDbusProvider, realm_dbus_provider, G_TYPE_OBJECT);

/**
 * realm_dbus_provider_emit_diagnostics:
 * @object: A #RealmDbusProvider.
 * @arg_data: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-realmd-Provider.Diagnostics">"Diagnostics"</link> D-Bus signal.
 */
void
realm_dbus_provider_emit_diagnostics (
    RealmDbusProvider *object,
    const gchar *arg_data)
{
  g_signal_emit_by_name (object, "diagnostics", arg_data);
}

/* ------------------------------------------------------------------------ */

/**
 * RealmDbusProviderProxy:
 *
 * The #RealmDbusProviderProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * RealmDbusProviderProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #RealmDbusProviderProxy.
 */

struct _RealmDbusProviderProxyPrivate
{
  GData *qdata;
};

static void realm_dbus_provider_proxy_iface_init (RealmDbusProviderIface *iface);

G_DEFINE_TYPE_WITH_CODE (RealmDbusProviderProxy, realm_dbus_provider_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (REALM_DBUS_TYPE_PROVIDER, realm_dbus_provider_proxy_iface_init));

static void
realm_dbus_provider_proxy_finalize (GObject *object)
{
  RealmDbusProviderProxy *proxy = REALM_DBUS_PROVIDER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (realm_dbus_provider_proxy_parent_class)->finalize (object);
}

static void
realm_dbus_provider_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
realm_dbus_provider_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
realm_dbus_provider_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_realm_dbus_provider_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], REALM_DBUS_TYPE_PROVIDER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, REALM_DBUS_TYPE_PROVIDER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
realm_dbus_provider_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  RealmDbusProviderProxy *proxy = REALM_DBUS_PROVIDER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_provider_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_provider_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
realm_dbus_provider_proxy_init (RealmDbusProviderProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, REALM_DBUS_TYPE_PROVIDER_PROXY, RealmDbusProviderProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), realm_dbus_provider_interface_info ());
}

static void
realm_dbus_provider_proxy_class_init (RealmDbusProviderProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (RealmDbusProviderProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = realm_dbus_provider_proxy_finalize;
  gobject_class->get_property = realm_dbus_provider_proxy_get_property;
  gobject_class->set_property = realm_dbus_provider_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = realm_dbus_provider_proxy_g_signal;
  proxy_class->g_properties_changed = realm_dbus_provider_proxy_g_properties_changed;

}

static void
realm_dbus_provider_proxy_iface_init (RealmDbusProviderIface *iface)
{
}

/**
 * realm_dbus_provider_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Provider.top_of_page">org.freedesktop.realmd.Provider</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_provider_proxy_new_finish() to get the result of the operation.
 *
 * See realm_dbus_provider_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
realm_dbus_provider_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (REALM_DBUS_TYPE_PROVIDER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Provider", NULL);
}

/**
 * realm_dbus_provider_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_provider_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with realm_dbus_provider_proxy_new().
 *
 * Returns: (transfer full) (type RealmDbusProviderProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusProvider *
realm_dbus_provider_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return REALM_DBUS_PROVIDER (ret);
  else
    return NULL;
}

/**
 * realm_dbus_provider_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Provider.top_of_page">org.freedesktop.realmd.Provider</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_provider_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type RealmDbusProviderProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusProvider *
realm_dbus_provider_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (REALM_DBUS_TYPE_PROVIDER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Provider", NULL);
  if (ret != NULL)
    return REALM_DBUS_PROVIDER (ret);
  else
    return NULL;
}


/**
 * realm_dbus_provider_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like realm_dbus_provider_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_provider_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See realm_dbus_provider_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
realm_dbus_provider_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (REALM_DBUS_TYPE_PROVIDER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Provider", NULL);
}

/**
 * realm_dbus_provider_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_provider_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with realm_dbus_provider_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type RealmDbusProviderProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusProvider *
realm_dbus_provider_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return REALM_DBUS_PROVIDER (ret);
  else
    return NULL;
}

/**
 * realm_dbus_provider_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like realm_dbus_provider_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_provider_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type RealmDbusProviderProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusProvider *
realm_dbus_provider_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (REALM_DBUS_TYPE_PROVIDER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Provider", NULL);
  if (ret != NULL)
    return REALM_DBUS_PROVIDER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * RealmDbusProviderSkeleton:
 *
 * The #RealmDbusProviderSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * RealmDbusProviderSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #RealmDbusProviderSkeleton.
 */

struct _RealmDbusProviderSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_realm_dbus_provider_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  RealmDbusProviderSkeleton *skeleton = REALM_DBUS_PROVIDER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], REALM_DBUS_TYPE_PROVIDER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, REALM_DBUS_TYPE_PROVIDER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_realm_dbus_provider_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  RealmDbusProviderSkeleton *skeleton = REALM_DBUS_PROVIDER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_provider_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_realm_dbus_provider_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  RealmDbusProviderSkeleton *skeleton = REALM_DBUS_PROVIDER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_provider_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _realm_dbus_provider_skeleton_vtable =
{
  _realm_dbus_provider_skeleton_handle_method_call,
  _realm_dbus_provider_skeleton_handle_get_property,
  _realm_dbus_provider_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
realm_dbus_provider_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return realm_dbus_provider_interface_info ();
}

static GDBusInterfaceVTable *
realm_dbus_provider_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_realm_dbus_provider_skeleton_vtable;
}

static GVariant *
realm_dbus_provider_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  RealmDbusProviderSkeleton *skeleton = REALM_DBUS_PROVIDER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_realm_dbus_provider_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _realm_dbus_provider_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _realm_dbus_provider_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _realm_dbus_provider_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.realmd.Provider", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
realm_dbus_provider_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_realm_dbus_provider_on_signal_diagnostics (
    RealmDbusProvider *object,
    const gchar *arg_data)
{
  RealmDbusProviderSkeleton *skeleton = REALM_DBUS_PROVIDER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s)",
                   arg_data));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.realmd.Provider", "Diagnostics",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void realm_dbus_provider_skeleton_iface_init (RealmDbusProviderIface *iface);
G_DEFINE_TYPE_WITH_CODE (RealmDbusProviderSkeleton, realm_dbus_provider_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (REALM_DBUS_TYPE_PROVIDER, realm_dbus_provider_skeleton_iface_init));

static void
realm_dbus_provider_skeleton_finalize (GObject *object)
{
  RealmDbusProviderSkeleton *skeleton = REALM_DBUS_PROVIDER_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (realm_dbus_provider_skeleton_parent_class)->finalize (object);
}

static void
realm_dbus_provider_skeleton_init (RealmDbusProviderSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, REALM_DBUS_TYPE_PROVIDER_SKELETON, RealmDbusProviderSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
realm_dbus_provider_skeleton_class_init (RealmDbusProviderSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (RealmDbusProviderSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = realm_dbus_provider_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = realm_dbus_provider_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = realm_dbus_provider_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = realm_dbus_provider_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = realm_dbus_provider_skeleton_dbus_interface_get_vtable;
}

static void
realm_dbus_provider_skeleton_iface_init (RealmDbusProviderIface *iface)
{
  iface->diagnostics = _realm_dbus_provider_on_signal_diagnostics;
}

/**
 * realm_dbus_provider_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Provider.top_of_page">org.freedesktop.realmd.Provider</link>.
 *
 * Returns: (transfer full) (type RealmDbusProviderSkeleton): The skeleton object.
 */
RealmDbusProvider *
realm_dbus_provider_skeleton_new (void)
{
  return REALM_DBUS_PROVIDER (g_object_new (REALM_DBUS_TYPE_PROVIDER_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.realmd.Kerberos
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:RealmDbusKerberos
 * @title: RealmDbusKerberos
 * @short_description: Generated C code for the org.freedesktop.realmd.Kerberos D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-realmd-Kerberos.top_of_page">org.freedesktop.realmd.Kerberos</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.realmd.Kerberos ---- */

static const _ExtendedGDBusArgInfo _realm_dbus_kerberos_method_info_discover_realm_IN_ARG_string =
{
  {
    -1,
    "string",
    "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_kerberos_method_info_discover_realm_IN_ARG_pointers[] =
{
  &_realm_dbus_kerberos_method_info_discover_realm_IN_ARG_string,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_kerberos_method_info_discover_realm_OUT_ARG_realm =
{
  {
    -1,
    "realm",
    "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_kerberos_method_info_discover_realm_OUT_ARG_discovered =
{
  {
    -1,
    "discovered",
    "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_kerberos_method_info_discover_realm_OUT_ARG_pointers[] =
{
  &_realm_dbus_kerberos_method_info_discover_realm_OUT_ARG_realm,
  &_realm_dbus_kerberos_method_info_discover_realm_OUT_ARG_discovered,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_kerberos_method_info_discover_realm =
{
  {
    -1,
    "DiscoverRealm",
    (GDBusArgInfo **) &_realm_dbus_kerberos_method_info_discover_realm_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_kerberos_method_info_discover_realm_OUT_ARG_pointers,
    NULL
  },
  "handle-discover-realm",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_kerberos_method_info_enroll_machine_with_kerberos_cache_IN_ARG_realm =
{
  {
    -1,
    "realm",
    "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_kerberos_method_info_enroll_machine_with_kerberos_cache_IN_ARG_kerberos_cache =
{
  {
    -1,
    "kerberos_cache",
    "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_kerberos_method_info_enroll_machine_with_kerberos_cache_IN_ARG_pointers[] =
{
  &_realm_dbus_kerberos_method_info_enroll_machine_with_kerberos_cache_IN_ARG_realm,
  &_realm_dbus_kerberos_method_info_enroll_machine_with_kerberos_cache_IN_ARG_kerberos_cache,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_kerberos_method_info_enroll_machine_with_kerberos_cache =
{
  {
    -1,
    "EnrollMachineWithKerberosCache",
    (GDBusArgInfo **) &_realm_dbus_kerberos_method_info_enroll_machine_with_kerberos_cache_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-enroll-machine-with-kerberos-cache",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_kerberos_method_info_unenroll_machine_with_kerberos_cache_IN_ARG_realm =
{
  {
    -1,
    "realm",
    "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_kerberos_method_info_unenroll_machine_with_kerberos_cache_IN_ARG_kerberos_cache =
{
  {
    -1,
    "kerberos_cache",
    "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_kerberos_method_info_unenroll_machine_with_kerberos_cache_IN_ARG_pointers[] =
{
  &_realm_dbus_kerberos_method_info_unenroll_machine_with_kerberos_cache_IN_ARG_realm,
  &_realm_dbus_kerberos_method_info_unenroll_machine_with_kerberos_cache_IN_ARG_kerberos_cache,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_kerberos_method_info_unenroll_machine_with_kerberos_cache =
{
  {
    -1,
    "UnenrollMachineWithKerberosCache",
    (GDBusArgInfo **) &_realm_dbus_kerberos_method_info_unenroll_machine_with_kerberos_cache_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-unenroll-machine-with-kerberos-cache",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _realm_dbus_kerberos_method_info_pointers[] =
{
  &_realm_dbus_kerberos_method_info_discover_realm,
  &_realm_dbus_kerberos_method_info_enroll_machine_with_kerberos_cache,
  &_realm_dbus_kerberos_method_info_unenroll_machine_with_kerberos_cache,
  NULL
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_kerberos_property_info_enrolled_realms =
{
  {
    -1,
    "EnrolledRealms",
    "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "enrolled-realms",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _realm_dbus_kerberos_property_info_pointers[] =
{
  &_realm_dbus_kerberos_property_info_enrolled_realms,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _realm_dbus_kerberos_interface_info =
{
  {
    -1,
    "org.freedesktop.realmd.Kerberos",
    (GDBusMethodInfo **) &_realm_dbus_kerberos_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_realm_dbus_kerberos_property_info_pointers,
    NULL
  },
  "kerberos",
};


/**
 * realm_dbus_kerberos_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-realmd-Kerberos.top_of_page">org.freedesktop.realmd.Kerberos</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
realm_dbus_kerberos_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_realm_dbus_kerberos_interface_info;
}

/**
 * realm_dbus_kerberos_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #RealmDbusKerberos interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
realm_dbus_kerberos_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "enrolled-realms");
  return property_id_begin - 1;
}



/**
 * RealmDbusKerberos:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Kerberos.top_of_page">org.freedesktop.realmd.Kerberos</link>.
 */

/**
 * RealmDbusKerberosIface:
 * @parent_iface: The parent interface.
 * @handle_discover_realm: Handler for the #RealmDbusKerberos::handle-discover-realm signal.
 * @handle_enroll_machine_with_kerberos_cache: Handler for the #RealmDbusKerberos::handle-enroll-machine-with-kerberos-cache signal.
 * @handle_unenroll_machine_with_kerberos_cache: Handler for the #RealmDbusKerberos::handle-unenroll-machine-with-kerberos-cache signal.
 * @get_enrolled_realms: Getter for the #RealmDbusKerberos:enrolled-realms property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Kerberos.top_of_page">org.freedesktop.realmd.Kerberos</link>.
 */

static void
realm_dbus_kerberos_default_init (RealmDbusKerberosIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * RealmDbusKerberos::handle-discover-realm:
   * @object: A #RealmDbusKerberos.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_string: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.DiscoverRealm">DiscoverRealm()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_kerberos_complete_discover_realm() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-discover-realm",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusKerberosIface, handle_discover_realm),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * RealmDbusKerberos::handle-enroll-machine-with-kerberos-cache:
   * @object: A #RealmDbusKerberos.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_realm: Argument passed by remote caller.
   * @arg_kerberos_cache: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.EnrollMachineWithKerberosCache">EnrollMachineWithKerberosCache()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_kerberos_complete_enroll_machine_with_kerberos_cache() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-enroll-machine-with-kerberos-cache",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusKerberosIface, handle_enroll_machine_with_kerberos_cache),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_VARIANT);

  /**
   * RealmDbusKerberos::handle-unenroll-machine-with-kerberos-cache:
   * @object: A #RealmDbusKerberos.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_realm: Argument passed by remote caller.
   * @arg_kerberos_cache: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.UnenrollMachineWithKerberosCache">UnenrollMachineWithKerberosCache()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_kerberos_complete_unenroll_machine_with_kerberos_cache() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-unenroll-machine-with-kerberos-cache",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusKerberosIface, handle_unenroll_machine_with_kerberos_cache),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_VARIANT);

  /* GObject properties for D-Bus properties: */
  /**
   * RealmDbusKerberos:enrolled-realms:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-realmd-Kerberos.EnrolledRealms">"EnrolledRealms"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("enrolled-realms", "EnrolledRealms", "EnrolledRealms", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

typedef RealmDbusKerberosIface RealmDbusKerberosInterface;
G_DEFINE_INTERFACE (RealmDbusKerberos, realm_dbus_kerberos, G_TYPE_OBJECT);

/**
 * realm_dbus_kerberos_get_enrolled_realms: (skip)
 * @object: A #RealmDbusKerberos.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-realmd-Kerberos.EnrolledRealms">"EnrolledRealms"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_kerberos_dup_enrolled_realms() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
realm_dbus_kerberos_get_enrolled_realms (RealmDbusKerberos *object)
{
  return REALM_DBUS_KERBEROS_GET_IFACE (object)->get_enrolled_realms (object);
}

/**
 * realm_dbus_kerberos_dup_enrolled_realms: (skip)
 * @object: A #RealmDbusKerberos.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-realmd-Kerberos.EnrolledRealms">"EnrolledRealms"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
realm_dbus_kerberos_dup_enrolled_realms (RealmDbusKerberos *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "enrolled-realms", &value, NULL);
  return value;
}

/**
 * realm_dbus_kerberos_set_enrolled_realms: (skip)
 * @object: A #RealmDbusKerberos.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-realmd-Kerberos.EnrolledRealms">"EnrolledRealms"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_kerberos_set_enrolled_realms (RealmDbusKerberos *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "enrolled-realms", value, NULL);
}

/**
 * realm_dbus_kerberos_call_discover_realm:
 * @proxy: A #RealmDbusKerberosProxy.
 * @arg_string: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.DiscoverRealm">DiscoverRealm()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_kerberos_call_discover_realm_finish() to get the result of the operation.
 *
 * See realm_dbus_kerberos_call_discover_realm_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_kerberos_call_discover_realm (
    RealmDbusKerberos *proxy,
    const gchar *arg_string,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "DiscoverRealm",
    g_variant_new ("(s)",
                   arg_string),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_kerberos_call_discover_realm_finish:
 * @proxy: A #RealmDbusKerberosProxy.
 * @out_realm: (out): Return location for return parameter or %NULL to ignore.
 * @out_discovered: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_kerberos_call_discover_realm().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_kerberos_call_discover_realm().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_kerberos_call_discover_realm_finish (
    RealmDbusKerberos *proxy,
    gchar **out_realm,
    GVariant **out_discovered,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s@a{sv})",
                 out_realm,
                 out_discovered);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_kerberos_call_discover_realm_sync:
 * @proxy: A #RealmDbusKerberosProxy.
 * @arg_string: Argument to pass with the method invocation.
 * @out_realm: (out): Return location for return parameter or %NULL to ignore.
 * @out_discovered: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.DiscoverRealm">DiscoverRealm()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_kerberos_call_discover_realm() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_kerberos_call_discover_realm_sync (
    RealmDbusKerberos *proxy,
    const gchar *arg_string,
    gchar **out_realm,
    GVariant **out_discovered,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "DiscoverRealm",
    g_variant_new ("(s)",
                   arg_string),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s@a{sv})",
                 out_realm,
                 out_discovered);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache:
 * @proxy: A #RealmDbusKerberosProxy.
 * @arg_realm: Argument to pass with the method invocation.
 * @arg_kerberos_cache: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.EnrollMachineWithKerberosCache">EnrollMachineWithKerberosCache()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache_finish() to get the result of the operation.
 *
 * See realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache (
    RealmDbusKerberos *proxy,
    const gchar *arg_realm,
    GVariant *arg_kerberos_cache,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "EnrollMachineWithKerberosCache",
    g_variant_new ("(s@ay)",
                   arg_realm,
                   arg_kerberos_cache),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache_finish:
 * @proxy: A #RealmDbusKerberosProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache_finish (
    RealmDbusKerberos *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache_sync:
 * @proxy: A #RealmDbusKerberosProxy.
 * @arg_realm: Argument to pass with the method invocation.
 * @arg_kerberos_cache: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.EnrollMachineWithKerberosCache">EnrollMachineWithKerberosCache()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_kerberos_call_enroll_machine_with_kerberos_cache_sync (
    RealmDbusKerberos *proxy,
    const gchar *arg_realm,
    GVariant *arg_kerberos_cache,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "EnrollMachineWithKerberosCache",
    g_variant_new ("(s@ay)",
                   arg_realm,
                   arg_kerberos_cache),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache:
 * @proxy: A #RealmDbusKerberosProxy.
 * @arg_realm: Argument to pass with the method invocation.
 * @arg_kerberos_cache: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.UnenrollMachineWithKerberosCache">UnenrollMachineWithKerberosCache()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache_finish() to get the result of the operation.
 *
 * See realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache (
    RealmDbusKerberos *proxy,
    const gchar *arg_realm,
    GVariant *arg_kerberos_cache,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "UnenrollMachineWithKerberosCache",
    g_variant_new ("(s@ay)",
                   arg_realm,
                   arg_kerberos_cache),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache_finish:
 * @proxy: A #RealmDbusKerberosProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache_finish (
    RealmDbusKerberos *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache_sync:
 * @proxy: A #RealmDbusKerberosProxy.
 * @arg_realm: Argument to pass with the method invocation.
 * @arg_kerberos_cache: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.UnenrollMachineWithKerberosCache">UnenrollMachineWithKerberosCache()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_kerberos_call_unenroll_machine_with_kerberos_cache_sync (
    RealmDbusKerberos *proxy,
    const gchar *arg_realm,
    GVariant *arg_kerberos_cache,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "UnenrollMachineWithKerberosCache",
    g_variant_new ("(s@ay)",
                   arg_realm,
                   arg_kerberos_cache),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_kerberos_complete_discover_realm:
 * @object: A #RealmDbusKerberos.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @realm: Parameter to return.
 * @discovered: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.DiscoverRealm">DiscoverRealm()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_kerberos_complete_discover_realm (
    RealmDbusKerberos *object,
    GDBusMethodInvocation *invocation,
    const gchar *realm,
    GVariant *discovered)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s@a{sv})",
                   realm,
                   discovered));
}

/**
 * realm_dbus_kerberos_complete_enroll_machine_with_kerberos_cache:
 * @object: A #RealmDbusKerberos.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.EnrollMachineWithKerberosCache">EnrollMachineWithKerberosCache()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_kerberos_complete_enroll_machine_with_kerberos_cache (
    RealmDbusKerberos *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_kerberos_complete_unenroll_machine_with_kerberos_cache:
 * @object: A #RealmDbusKerberos.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-realmd-Kerberos.UnenrollMachineWithKerberosCache">UnenrollMachineWithKerberosCache()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_kerberos_complete_unenroll_machine_with_kerberos_cache (
    RealmDbusKerberos *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * RealmDbusKerberosProxy:
 *
 * The #RealmDbusKerberosProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * RealmDbusKerberosProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #RealmDbusKerberosProxy.
 */

struct _RealmDbusKerberosProxyPrivate
{
  GData *qdata;
};

static void realm_dbus_kerberos_proxy_iface_init (RealmDbusKerberosIface *iface);

G_DEFINE_TYPE_WITH_CODE (RealmDbusKerberosProxy, realm_dbus_kerberos_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (REALM_DBUS_TYPE_KERBEROS, realm_dbus_kerberos_proxy_iface_init));

static void
realm_dbus_kerberos_proxy_finalize (GObject *object)
{
  RealmDbusKerberosProxy *proxy = REALM_DBUS_KERBEROS_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (realm_dbus_kerberos_proxy_parent_class)->finalize (object);
}

static void
realm_dbus_kerberos_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  info = _realm_dbus_kerberos_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
realm_dbus_kerberos_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  error = NULL;
  if (!g_dbus_proxy_call_finish (proxy, res, &error))
    {
      g_warning ("Error setting property `%s' on interface org.freedesktop.realmd.Kerberos: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
}

static void
realm_dbus_kerberos_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  info = _realm_dbus_kerberos_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.freedesktop.realmd.Kerberos", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) realm_dbus_kerberos_proxy_set_property_cb, (gpointer) info);
  g_variant_unref (variant);
}

static void
realm_dbus_kerberos_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_realm_dbus_kerberos_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], REALM_DBUS_TYPE_KERBEROS);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, REALM_DBUS_TYPE_KERBEROS);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
realm_dbus_kerberos_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  RealmDbusKerberosProxy *proxy = REALM_DBUS_KERBEROS_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_kerberos_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_kerberos_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *const *
realm_dbus_kerberos_proxy_get_enrolled_realms (RealmDbusKerberos *object)
{
  RealmDbusKerberosProxy *proxy = REALM_DBUS_KERBEROS_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "EnrolledRealms");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "EnrolledRealms");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "EnrolledRealms", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static void
realm_dbus_kerberos_proxy_init (RealmDbusKerberosProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, REALM_DBUS_TYPE_KERBEROS_PROXY, RealmDbusKerberosProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), realm_dbus_kerberos_interface_info ());
}

static void
realm_dbus_kerberos_proxy_class_init (RealmDbusKerberosProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (RealmDbusKerberosProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = realm_dbus_kerberos_proxy_finalize;
  gobject_class->get_property = realm_dbus_kerberos_proxy_get_property;
  gobject_class->set_property = realm_dbus_kerberos_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = realm_dbus_kerberos_proxy_g_signal;
  proxy_class->g_properties_changed = realm_dbus_kerberos_proxy_g_properties_changed;


  realm_dbus_kerberos_override_properties (gobject_class, 1);
}

static void
realm_dbus_kerberos_proxy_iface_init (RealmDbusKerberosIface *iface)
{
  iface->get_enrolled_realms = realm_dbus_kerberos_proxy_get_enrolled_realms;
}

/**
 * realm_dbus_kerberos_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Kerberos.top_of_page">org.freedesktop.realmd.Kerberos</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_kerberos_proxy_new_finish() to get the result of the operation.
 *
 * See realm_dbus_kerberos_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
realm_dbus_kerberos_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (REALM_DBUS_TYPE_KERBEROS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Kerberos", NULL);
}

/**
 * realm_dbus_kerberos_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_kerberos_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with realm_dbus_kerberos_proxy_new().
 *
 * Returns: (transfer full) (type RealmDbusKerberosProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusKerberos *
realm_dbus_kerberos_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return REALM_DBUS_KERBEROS (ret);
  else
    return NULL;
}

/**
 * realm_dbus_kerberos_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Kerberos.top_of_page">org.freedesktop.realmd.Kerberos</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_kerberos_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type RealmDbusKerberosProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusKerberos *
realm_dbus_kerberos_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (REALM_DBUS_TYPE_KERBEROS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Kerberos", NULL);
  if (ret != NULL)
    return REALM_DBUS_KERBEROS (ret);
  else
    return NULL;
}


/**
 * realm_dbus_kerberos_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like realm_dbus_kerberos_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_kerberos_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See realm_dbus_kerberos_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
realm_dbus_kerberos_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (REALM_DBUS_TYPE_KERBEROS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Kerberos", NULL);
}

/**
 * realm_dbus_kerberos_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_kerberos_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with realm_dbus_kerberos_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type RealmDbusKerberosProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusKerberos *
realm_dbus_kerberos_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return REALM_DBUS_KERBEROS (ret);
  else
    return NULL;
}

/**
 * realm_dbus_kerberos_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like realm_dbus_kerberos_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_kerberos_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type RealmDbusKerberosProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusKerberos *
realm_dbus_kerberos_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (REALM_DBUS_TYPE_KERBEROS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.realmd.Kerberos", NULL);
  if (ret != NULL)
    return REALM_DBUS_KERBEROS (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * RealmDbusKerberosSkeleton:
 *
 * The #RealmDbusKerberosSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * RealmDbusKerberosSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #RealmDbusKerberosSkeleton.
 */

struct _RealmDbusKerberosSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_realm_dbus_kerberos_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], REALM_DBUS_TYPE_KERBEROS);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, REALM_DBUS_TYPE_KERBEROS);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_realm_dbus_kerberos_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_kerberos_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_realm_dbus_kerberos_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_kerberos_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _realm_dbus_kerberos_skeleton_vtable =
{
  _realm_dbus_kerberos_skeleton_handle_method_call,
  _realm_dbus_kerberos_skeleton_handle_get_property,
  _realm_dbus_kerberos_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
realm_dbus_kerberos_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return realm_dbus_kerberos_interface_info ();
}

static GDBusInterfaceVTable *
realm_dbus_kerberos_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_realm_dbus_kerberos_skeleton_vtable;
}

static GVariant *
realm_dbus_kerberos_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_realm_dbus_kerberos_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _realm_dbus_kerberos_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _realm_dbus_kerberos_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _realm_dbus_kerberos_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.realmd.Kerberos", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _realm_dbus_kerberos_emit_changed (gpointer user_data);

static void
realm_dbus_kerberos_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _realm_dbus_kerberos_emit_changed (skeleton);
}

static void realm_dbus_kerberos_skeleton_iface_init (RealmDbusKerberosIface *iface);
G_DEFINE_TYPE_WITH_CODE (RealmDbusKerberosSkeleton, realm_dbus_kerberos_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (REALM_DBUS_TYPE_KERBEROS, realm_dbus_kerberos_skeleton_iface_init));

static void
realm_dbus_kerberos_skeleton_finalize (GObject *object)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (object);
  guint n;
  for (n = 0; n < 1; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (realm_dbus_kerberos_skeleton_parent_class)->finalize (object);
}

static void
realm_dbus_kerberos_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_realm_dbus_kerberos_emit_changed (gpointer user_data)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *l;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.freedesktop.realmd.Kerberos",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (l = connections; l != NULL; l = l->next)
        {
          GDBusConnection *connection = l->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_realm_dbus_kerberos_schedule_emit_changed (RealmDbusKerberosSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
realm_dbus_kerberos_skeleton_notify (GObject      *object,
  GParamSpec *pspec)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _realm_dbus_kerberos_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
realm_dbus_kerberos_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _realm_dbus_kerberos_schedule_emit_changed (skeleton, _realm_dbus_kerberos_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
realm_dbus_kerberos_skeleton_init (RealmDbusKerberosSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, REALM_DBUS_TYPE_KERBEROS_SKELETON, RealmDbusKerberosSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 1);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRV);
}

static const gchar *const *
realm_dbus_kerberos_skeleton_get_enrolled_realms (RealmDbusKerberos *object)
{
  RealmDbusKerberosSkeleton *skeleton = REALM_DBUS_KERBEROS_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
realm_dbus_kerberos_skeleton_class_init (RealmDbusKerberosSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (RealmDbusKerberosSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = realm_dbus_kerberos_skeleton_finalize;
  gobject_class->get_property = realm_dbus_kerberos_skeleton_get_property;
  gobject_class->set_property = realm_dbus_kerberos_skeleton_set_property;
  gobject_class->notify       = realm_dbus_kerberos_skeleton_notify;


  realm_dbus_kerberos_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = realm_dbus_kerberos_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = realm_dbus_kerberos_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = realm_dbus_kerberos_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = realm_dbus_kerberos_skeleton_dbus_interface_get_vtable;
}

static void
realm_dbus_kerberos_skeleton_iface_init (RealmDbusKerberosIface *iface)
{
  iface->get_enrolled_realms = realm_dbus_kerberos_skeleton_get_enrolled_realms;
}

/**
 * realm_dbus_kerberos_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-realmd-Kerberos.top_of_page">org.freedesktop.realmd.Kerberos</link>.
 *
 * Returns: (transfer full) (type RealmDbusKerberosSkeleton): The skeleton object.
 */
RealmDbusKerberos *
realm_dbus_kerberos_skeleton_new (void)
{
  return REALM_DBUS_KERBEROS (g_object_new (REALM_DBUS_TYPE_KERBEROS_SKELETON, NULL));
}

