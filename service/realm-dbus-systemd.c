/*
 * Generated by gdbus-codegen 2.33.1. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "realm-dbus-systemd.h"

#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        ret = (g_value_get_double (a) == g_value_get_double (b));
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.systemd1.Manager
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:RealmDbusSystemdManager
 * @title: RealmDbusSystemdManager
 * @short_description: Generated C code for the org.freedesktop.systemd1.Manager D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-systemd1-Manager.top_of_page">org.freedesktop.systemd1.Manager</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.systemd1.Manager ---- */

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_get_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_get_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_unit_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_get_unit_OUT_ARG_unit =
{
  {
    -1,
    (gchar *) "unit",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_get_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_unit_OUT_ARG_unit,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_get_unit =
{
  {
    -1,
    (gchar *) "GetUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_get_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_get_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-get-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_get_unit_by_pid_IN_ARG_pid =
{
  {
    -1,
    (gchar *) "pid",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_get_unit_by_pid_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_unit_by_pid_IN_ARG_pid,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_get_unit_by_pid_OUT_ARG_unit =
{
  {
    -1,
    (gchar *) "unit",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_get_unit_by_pid_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_unit_by_pid_OUT_ARG_unit,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_get_unit_by_pid =
{
  {
    -1,
    (gchar *) "GetUnitByPID",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_get_unit_by_pid_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_get_unit_by_pid_OUT_ARG_pointers,
    NULL
  },
  "handle-get-unit-by-pid",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_load_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_load_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_load_unit_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_load_unit_OUT_ARG_unit =
{
  {
    -1,
    (gchar *) "unit",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_load_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_load_unit_OUT_ARG_unit,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_load_unit =
{
  {
    -1,
    (gchar *) "LoadUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_load_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_load_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-load-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_start_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_start_unit_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_start_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_start_unit_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_start_unit_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_start_unit_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_start_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_start_unit_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_start_unit =
{
  {
    -1,
    (gchar *) "StartUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_start_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_start_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-start-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_start_unit_replace_IN_ARG_old_unit =
{
  {
    -1,
    (gchar *) "old_unit",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_start_unit_replace_IN_ARG_new_unit =
{
  {
    -1,
    (gchar *) "new_unit",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_start_unit_replace_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_start_unit_replace_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_start_unit_replace_IN_ARG_old_unit,
  &_realm_dbus_systemd_manager_method_info_start_unit_replace_IN_ARG_new_unit,
  &_realm_dbus_systemd_manager_method_info_start_unit_replace_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_start_unit_replace_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_start_unit_replace_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_start_unit_replace_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_start_unit_replace =
{
  {
    -1,
    (gchar *) "StartUnitReplace",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_start_unit_replace_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_start_unit_replace_OUT_ARG_pointers,
    NULL
  },
  "handle-start-unit-replace",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_stop_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_stop_unit_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_stop_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_stop_unit_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_stop_unit_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_stop_unit_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_stop_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_stop_unit_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_stop_unit =
{
  {
    -1,
    (gchar *) "StopUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_stop_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_stop_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-stop-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_unit_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reload_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reload_unit_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_reload_unit_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_unit_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reload_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reload_unit_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reload_unit =
{
  {
    -1,
    (gchar *) "ReloadUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reload_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reload_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-reload-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_restart_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_restart_unit_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_restart_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_restart_unit_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_restart_unit_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_restart_unit_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_restart_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_restart_unit_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_restart_unit =
{
  {
    -1,
    (gchar *) "RestartUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_restart_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_restart_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-restart-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_try_restart_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_try_restart_unit_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_try_restart_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_try_restart_unit_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_try_restart_unit_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_try_restart_unit_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_try_restart_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_try_restart_unit_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_try_restart_unit =
{
  {
    -1,
    (gchar *) "TryRestartUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_try_restart_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_try_restart_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-try-restart-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_or_restart_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_or_restart_unit_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reload_or_restart_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reload_or_restart_unit_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_reload_or_restart_unit_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_or_restart_unit_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reload_or_restart_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reload_or_restart_unit_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reload_or_restart_unit =
{
  {
    -1,
    (gchar *) "ReloadOrRestartUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reload_or_restart_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reload_or_restart_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-reload-or-restart-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit =
{
  {
    -1,
    (gchar *) "ReloadOrTryRestartUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit_OUT_ARG_pointers,
    NULL
  },
  "handle-reload-or-try-restart-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_who =
{
  {
    -1,
    (gchar *) "who",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_signal =
{
  {
    -1,
    (gchar *) "signal",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_who,
  &_realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_mode,
  &_realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_signal,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_kill_unit =
{
  {
    -1,
    (gchar *) "KillUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_kill_unit_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-kill-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reset_failed_unit_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reset_failed_unit_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reset_failed_unit_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reset_failed_unit =
{
  {
    -1,
    (gchar *) "ResetFailedUnit",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reset_failed_unit_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-reset-failed-unit",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_get_job_IN_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_get_job_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_job_IN_ARG_id,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_get_job_OUT_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_get_job_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_job_OUT_ARG_job,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_get_job =
{
  {
    -1,
    (gchar *) "GetJob",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_get_job_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_get_job_OUT_ARG_pointers,
    NULL
  },
  "handle-get-job",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_clear_jobs =
{
  {
    -1,
    (gchar *) "ClearJobs",
    NULL,
    NULL,
    NULL
  },
  "handle-clear-jobs",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reset_failed =
{
  {
    -1,
    (gchar *) "ResetFailed",
    NULL,
    NULL,
    NULL
  },
  "handle-reset-failed",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_list_units_OUT_ARG_units =
{
  {
    -1,
    (gchar *) "units",
    (gchar *) "a(ssssssouso)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_list_units_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_list_units_OUT_ARG_units,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_list_units =
{
  {
    -1,
    (gchar *) "ListUnits",
    NULL,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_list_units_OUT_ARG_pointers,
    NULL
  },
  "handle-list-units",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_list_jobs_OUT_ARG_jobs =
{
  {
    -1,
    (gchar *) "jobs",
    (gchar *) "a(usssoo)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_list_jobs_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_list_jobs_OUT_ARG_jobs,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_list_jobs =
{
  {
    -1,
    (gchar *) "ListJobs",
    NULL,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_list_jobs_OUT_ARG_pointers,
    NULL
  },
  "handle-list-jobs",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_subscribe =
{
  {
    -1,
    (gchar *) "Subscribe",
    NULL,
    NULL,
    NULL
  },
  "handle-subscribe",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_unsubscribe =
{
  {
    -1,
    (gchar *) "Unsubscribe",
    NULL,
    NULL,
    NULL
  },
  "handle-unsubscribe",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_dump =
{
  {
    -1,
    (gchar *) "Dump",
    NULL,
    NULL,
    NULL
  },
  "handle-dump",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_create_snapshot_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_create_snapshot_IN_ARG_cleanup =
{
  {
    -1,
    (gchar *) "cleanup",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_create_snapshot_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_create_snapshot_IN_ARG_name,
  &_realm_dbus_systemd_manager_method_info_create_snapshot_IN_ARG_cleanup,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_create_snapshot_OUT_ARG_unit =
{
  {
    -1,
    (gchar *) "unit",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_create_snapshot_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_create_snapshot_OUT_ARG_unit,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_create_snapshot =
{
  {
    -1,
    (gchar *) "CreateSnapshot",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_create_snapshot_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_create_snapshot_OUT_ARG_pointers,
    NULL
  },
  "handle-create-snapshot",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reload =
{
  {
    -1,
    (gchar *) "Reload",
    NULL,
    NULL,
    NULL
  },
  "handle-reload",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reexecute =
{
  {
    -1,
    (gchar *) "Reexecute",
    NULL,
    NULL,
    NULL
  },
  "handle-reexecute",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_exit =
{
  {
    -1,
    (gchar *) "Exit",
    NULL,
    NULL,
    NULL
  },
  "handle-exit",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reboot =
{
  {
    -1,
    (gchar *) "Reboot",
    NULL,
    NULL,
    NULL
  },
  "handle-reboot",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_power_off =
{
  {
    -1,
    (gchar *) "PowerOff",
    NULL,
    NULL,
    NULL
  },
  "handle-power-off",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_halt =
{
  {
    -1,
    (gchar *) "Halt",
    NULL,
    NULL,
    NULL
  },
  "handle-halt",
  FALSE
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_kexec =
{
  {
    -1,
    (gchar *) "KExec",
    NULL,
    NULL,
    NULL
  },
  "handle-kexec",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_set_environment_IN_ARG_names =
{
  {
    -1,
    (gchar *) "names",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_set_environment_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_set_environment_IN_ARG_names,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_set_environment =
{
  {
    -1,
    (gchar *) "SetEnvironment",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_set_environment_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-environment",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_unset_environment_IN_ARG_names =
{
  {
    -1,
    (gchar *) "names",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_unset_environment_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_unset_environment_IN_ARG_names,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_unset_environment =
{
  {
    -1,
    (gchar *) "UnsetEnvironment",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_unset_environment_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-unset-environment",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_unset_and_set_environment_IN_ARG_unset =
{
  {
    -1,
    (gchar *) "unset",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_unset_and_set_environment_IN_ARG_set =
{
  {
    -1,
    (gchar *) "set",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_unset_and_set_environment_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_unset_and_set_environment_IN_ARG_unset,
  &_realm_dbus_systemd_manager_method_info_unset_and_set_environment_IN_ARG_set,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_unset_and_set_environment =
{
  {
    -1,
    (gchar *) "UnsetAndSetEnvironment",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_unset_and_set_environment_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-unset-and-set-environment",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_list_unit_files_OUT_ARG_changes =
{
  {
    -1,
    (gchar *) "changes",
    (gchar *) "a(ss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_list_unit_files_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_list_unit_files_OUT_ARG_changes,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_list_unit_files =
{
  {
    -1,
    (gchar *) "ListUnitFiles",
    NULL,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_list_unit_files_OUT_ARG_pointers,
    NULL
  },
  "handle-list-unit-files",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_get_unit_file_state_IN_ARG_file =
{
  {
    -1,
    (gchar *) "file",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_get_unit_file_state_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_unit_file_state_IN_ARG_file,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_get_unit_file_state_OUT_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_get_unit_file_state_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_unit_file_state_OUT_ARG_state,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_get_unit_file_state =
{
  {
    -1,
    (gchar *) "GetUnitFileState",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_get_unit_file_state_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_get_unit_file_state_OUT_ARG_pointers,
    NULL
  },
  "handle-get-unit-file-state",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_enable_unit_files_IN_ARG_files =
{
  {
    -1,
    (gchar *) "files",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_enable_unit_files_IN_ARG_runtime =
{
  {
    -1,
    (gchar *) "runtime",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_enable_unit_files_IN_ARG_force =
{
  {
    -1,
    (gchar *) "force",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_enable_unit_files_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_enable_unit_files_IN_ARG_files,
  &_realm_dbus_systemd_manager_method_info_enable_unit_files_IN_ARG_runtime,
  &_realm_dbus_systemd_manager_method_info_enable_unit_files_IN_ARG_force,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_enable_unit_files_OUT_ARG_carries_install_info =
{
  {
    -1,
    (gchar *) "carries_install_info",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_enable_unit_files_OUT_ARG_changes =
{
  {
    -1,
    (gchar *) "changes",
    (gchar *) "a(sss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_enable_unit_files_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_enable_unit_files_OUT_ARG_carries_install_info,
  &_realm_dbus_systemd_manager_method_info_enable_unit_files_OUT_ARG_changes,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_enable_unit_files =
{
  {
    -1,
    (gchar *) "EnableUnitFiles",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_enable_unit_files_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_enable_unit_files_OUT_ARG_pointers,
    NULL
  },
  "handle-enable-unit-files",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_disable_unit_files_IN_ARG_files =
{
  {
    -1,
    (gchar *) "files",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_disable_unit_files_IN_ARG_runtime =
{
  {
    -1,
    (gchar *) "runtime",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_disable_unit_files_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_disable_unit_files_IN_ARG_files,
  &_realm_dbus_systemd_manager_method_info_disable_unit_files_IN_ARG_runtime,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_disable_unit_files_OUT_ARG_changes =
{
  {
    -1,
    (gchar *) "changes",
    (gchar *) "a(sss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_disable_unit_files_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_disable_unit_files_OUT_ARG_changes,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_disable_unit_files =
{
  {
    -1,
    (gchar *) "DisableUnitFiles",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_disable_unit_files_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_disable_unit_files_OUT_ARG_pointers,
    NULL
  },
  "handle-disable-unit-files",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reenable_unit_files_IN_ARG_files =
{
  {
    -1,
    (gchar *) "files",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reenable_unit_files_IN_ARG_runtime =
{
  {
    -1,
    (gchar *) "runtime",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reenable_unit_files_IN_ARG_force =
{
  {
    -1,
    (gchar *) "force",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reenable_unit_files_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reenable_unit_files_IN_ARG_files,
  &_realm_dbus_systemd_manager_method_info_reenable_unit_files_IN_ARG_runtime,
  &_realm_dbus_systemd_manager_method_info_reenable_unit_files_IN_ARG_force,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reenable_unit_files_OUT_ARG_carries_install_info =
{
  {
    -1,
    (gchar *) "carries_install_info",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_reenable_unit_files_OUT_ARG_changes =
{
  {
    -1,
    (gchar *) "changes",
    (gchar *) "a(sss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_reenable_unit_files_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_reenable_unit_files_OUT_ARG_carries_install_info,
  &_realm_dbus_systemd_manager_method_info_reenable_unit_files_OUT_ARG_changes,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_reenable_unit_files =
{
  {
    -1,
    (gchar *) "ReenableUnitFiles",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reenable_unit_files_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_reenable_unit_files_OUT_ARG_pointers,
    NULL
  },
  "handle-reenable-unit-files",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_link_unit_files_IN_ARG_files =
{
  {
    -1,
    (gchar *) "files",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_link_unit_files_IN_ARG_runtime =
{
  {
    -1,
    (gchar *) "runtime",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_link_unit_files_IN_ARG_force =
{
  {
    -1,
    (gchar *) "force",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_link_unit_files_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_link_unit_files_IN_ARG_files,
  &_realm_dbus_systemd_manager_method_info_link_unit_files_IN_ARG_runtime,
  &_realm_dbus_systemd_manager_method_info_link_unit_files_IN_ARG_force,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_link_unit_files_OUT_ARG_changes =
{
  {
    -1,
    (gchar *) "changes",
    (gchar *) "a(sss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_link_unit_files_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_link_unit_files_OUT_ARG_changes,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_link_unit_files =
{
  {
    -1,
    (gchar *) "LinkUnitFiles",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_link_unit_files_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_link_unit_files_OUT_ARG_pointers,
    NULL
  },
  "handle-link-unit-files",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_preset_unit_files_IN_ARG_files =
{
  {
    -1,
    (gchar *) "files",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_preset_unit_files_IN_ARG_runtime =
{
  {
    -1,
    (gchar *) "runtime",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_preset_unit_files_IN_ARG_force =
{
  {
    -1,
    (gchar *) "force",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_preset_unit_files_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_preset_unit_files_IN_ARG_files,
  &_realm_dbus_systemd_manager_method_info_preset_unit_files_IN_ARG_runtime,
  &_realm_dbus_systemd_manager_method_info_preset_unit_files_IN_ARG_force,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_preset_unit_files_OUT_ARG_carries_install_info =
{
  {
    -1,
    (gchar *) "carries_install_info",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_preset_unit_files_OUT_ARG_changes =
{
  {
    -1,
    (gchar *) "changes",
    (gchar *) "a(sss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_preset_unit_files_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_preset_unit_files_OUT_ARG_carries_install_info,
  &_realm_dbus_systemd_manager_method_info_preset_unit_files_OUT_ARG_changes,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_preset_unit_files =
{
  {
    -1,
    (gchar *) "PresetUnitFiles",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_preset_unit_files_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_preset_unit_files_OUT_ARG_pointers,
    NULL
  },
  "handle-preset-unit-files",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_mask_unit_files_IN_ARG_files =
{
  {
    -1,
    (gchar *) "files",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_mask_unit_files_IN_ARG_runtime =
{
  {
    -1,
    (gchar *) "runtime",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_mask_unit_files_IN_ARG_force =
{
  {
    -1,
    (gchar *) "force",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_mask_unit_files_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_mask_unit_files_IN_ARG_files,
  &_realm_dbus_systemd_manager_method_info_mask_unit_files_IN_ARG_runtime,
  &_realm_dbus_systemd_manager_method_info_mask_unit_files_IN_ARG_force,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_mask_unit_files_OUT_ARG_changes =
{
  {
    -1,
    (gchar *) "changes",
    (gchar *) "a(sss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_mask_unit_files_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_mask_unit_files_OUT_ARG_changes,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_mask_unit_files =
{
  {
    -1,
    (gchar *) "MaskUnitFiles",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_mask_unit_files_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_mask_unit_files_OUT_ARG_pointers,
    NULL
  },
  "handle-mask-unit-files",
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_unmask_unit_files_IN_ARG_files =
{
  {
    -1,
    (gchar *) "files",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_unmask_unit_files_IN_ARG_runtime =
{
  {
    -1,
    (gchar *) "runtime",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_unmask_unit_files_IN_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_unmask_unit_files_IN_ARG_files,
  &_realm_dbus_systemd_manager_method_info_unmask_unit_files_IN_ARG_runtime,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_method_info_unmask_unit_files_OUT_ARG_changes =
{
  {
    -1,
    (gchar *) "changes",
    (gchar *) "a(sss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_method_info_unmask_unit_files_OUT_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_unmask_unit_files_OUT_ARG_changes,
  NULL
};

static const _ExtendedGDBusMethodInfo _realm_dbus_systemd_manager_method_info_unmask_unit_files =
{
  {
    -1,
    (gchar *) "UnmaskUnitFiles",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_unmask_unit_files_IN_ARG_pointers,
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_method_info_unmask_unit_files_OUT_ARG_pointers,
    NULL
  },
  "handle-unmask-unit-files",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _realm_dbus_systemd_manager_method_info_pointers[] =
{
  &_realm_dbus_systemd_manager_method_info_get_unit,
  &_realm_dbus_systemd_manager_method_info_get_unit_by_pid,
  &_realm_dbus_systemd_manager_method_info_load_unit,
  &_realm_dbus_systemd_manager_method_info_start_unit,
  &_realm_dbus_systemd_manager_method_info_start_unit_replace,
  &_realm_dbus_systemd_manager_method_info_stop_unit,
  &_realm_dbus_systemd_manager_method_info_reload_unit,
  &_realm_dbus_systemd_manager_method_info_restart_unit,
  &_realm_dbus_systemd_manager_method_info_try_restart_unit,
  &_realm_dbus_systemd_manager_method_info_reload_or_restart_unit,
  &_realm_dbus_systemd_manager_method_info_reload_or_try_restart_unit,
  &_realm_dbus_systemd_manager_method_info_kill_unit,
  &_realm_dbus_systemd_manager_method_info_reset_failed_unit,
  &_realm_dbus_systemd_manager_method_info_get_job,
  &_realm_dbus_systemd_manager_method_info_clear_jobs,
  &_realm_dbus_systemd_manager_method_info_reset_failed,
  &_realm_dbus_systemd_manager_method_info_list_units,
  &_realm_dbus_systemd_manager_method_info_list_jobs,
  &_realm_dbus_systemd_manager_method_info_subscribe,
  &_realm_dbus_systemd_manager_method_info_unsubscribe,
  &_realm_dbus_systemd_manager_method_info_dump,
  &_realm_dbus_systemd_manager_method_info_create_snapshot,
  &_realm_dbus_systemd_manager_method_info_reload,
  &_realm_dbus_systemd_manager_method_info_reexecute,
  &_realm_dbus_systemd_manager_method_info_exit,
  &_realm_dbus_systemd_manager_method_info_reboot,
  &_realm_dbus_systemd_manager_method_info_power_off,
  &_realm_dbus_systemd_manager_method_info_halt,
  &_realm_dbus_systemd_manager_method_info_kexec,
  &_realm_dbus_systemd_manager_method_info_set_environment,
  &_realm_dbus_systemd_manager_method_info_unset_environment,
  &_realm_dbus_systemd_manager_method_info_unset_and_set_environment,
  &_realm_dbus_systemd_manager_method_info_list_unit_files,
  &_realm_dbus_systemd_manager_method_info_get_unit_file_state,
  &_realm_dbus_systemd_manager_method_info_enable_unit_files,
  &_realm_dbus_systemd_manager_method_info_disable_unit_files,
  &_realm_dbus_systemd_manager_method_info_reenable_unit_files,
  &_realm_dbus_systemd_manager_method_info_link_unit_files,
  &_realm_dbus_systemd_manager_method_info_preset_unit_files,
  &_realm_dbus_systemd_manager_method_info_mask_unit_files,
  &_realm_dbus_systemd_manager_method_info_unmask_unit_files,
  NULL
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_unit_new_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_unit_new_ARG_unit =
{
  {
    -1,
    (gchar *) "unit",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_signal_info_unit_new_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_signal_info_unit_new_ARG_id,
  &_realm_dbus_systemd_manager_signal_info_unit_new_ARG_unit,
  NULL
};

static const _ExtendedGDBusSignalInfo _realm_dbus_systemd_manager_signal_info_unit_new =
{
  {
    -1,
    (gchar *) "UnitNew",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_signal_info_unit_new_ARG_pointers,
    NULL
  },
  "unit-new"
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_unit_removed_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_unit_removed_ARG_unit =
{
  {
    -1,
    (gchar *) "unit",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_signal_info_unit_removed_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_signal_info_unit_removed_ARG_id,
  &_realm_dbus_systemd_manager_signal_info_unit_removed_ARG_unit,
  NULL
};

static const _ExtendedGDBusSignalInfo _realm_dbus_systemd_manager_signal_info_unit_removed =
{
  {
    -1,
    (gchar *) "UnitRemoved",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_signal_info_unit_removed_ARG_pointers,
    NULL
  },
  "unit-removed"
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_job_new_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_job_new_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_signal_info_job_new_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_signal_info_job_new_ARG_id,
  &_realm_dbus_systemd_manager_signal_info_job_new_ARG_job,
  NULL
};

static const _ExtendedGDBusSignalInfo _realm_dbus_systemd_manager_signal_info_job_new =
{
  {
    -1,
    (gchar *) "JobNew",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_signal_info_job_new_ARG_pointers,
    NULL
  },
  "job-new"
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_job_removed_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_job_removed_ARG_job =
{
  {
    -1,
    (gchar *) "job",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_job_removed_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_signal_info_job_removed_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_signal_info_job_removed_ARG_id,
  &_realm_dbus_systemd_manager_signal_info_job_removed_ARG_job,
  &_realm_dbus_systemd_manager_signal_info_job_removed_ARG_result,
  NULL
};

static const _ExtendedGDBusSignalInfo _realm_dbus_systemd_manager_signal_info_job_removed =
{
  {
    -1,
    (gchar *) "JobRemoved",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_signal_info_job_removed_ARG_pointers,
    NULL
  },
  "job-removed"
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_startup_finished_ARG_kernel =
{
  {
    -1,
    (gchar *) "kernel",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_startup_finished_ARG_initrd =
{
  {
    -1,
    (gchar *) "initrd",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_startup_finished_ARG_userspace =
{
  {
    -1,
    (gchar *) "userspace",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _realm_dbus_systemd_manager_signal_info_startup_finished_ARG_total =
{
  {
    -1,
    (gchar *) "total",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _realm_dbus_systemd_manager_signal_info_startup_finished_ARG_pointers[] =
{
  &_realm_dbus_systemd_manager_signal_info_startup_finished_ARG_kernel,
  &_realm_dbus_systemd_manager_signal_info_startup_finished_ARG_initrd,
  &_realm_dbus_systemd_manager_signal_info_startup_finished_ARG_userspace,
  &_realm_dbus_systemd_manager_signal_info_startup_finished_ARG_total,
  NULL
};

static const _ExtendedGDBusSignalInfo _realm_dbus_systemd_manager_signal_info_startup_finished =
{
  {
    -1,
    (gchar *) "StartupFinished",
    (GDBusArgInfo **) &_realm_dbus_systemd_manager_signal_info_startup_finished_ARG_pointers,
    NULL
  },
  "startup-finished"
};

static const _ExtendedGDBusSignalInfo _realm_dbus_systemd_manager_signal_info_unit_files_changed =
{
  {
    -1,
    (gchar *) "UnitFilesChanged",
    NULL,
    NULL
  },
  "unit-files-changed"
};

static const _ExtendedGDBusSignalInfo * const _realm_dbus_systemd_manager_signal_info_pointers[] =
{
  &_realm_dbus_systemd_manager_signal_info_unit_new,
  &_realm_dbus_systemd_manager_signal_info_unit_removed,
  &_realm_dbus_systemd_manager_signal_info_job_new,
  &_realm_dbus_systemd_manager_signal_info_job_removed,
  &_realm_dbus_systemd_manager_signal_info_startup_finished,
  &_realm_dbus_systemd_manager_signal_info_unit_files_changed,
  NULL
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_version =
{
  {
    -1,
    (gchar *) "Version",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "version",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_distribution =
{
  {
    -1,
    (gchar *) "Distribution",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "distribution",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_features =
{
  {
    -1,
    (gchar *) "Features",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "features",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_tainted =
{
  {
    -1,
    (gchar *) "Tainted",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "tainted",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_running_as =
{
  {
    -1,
    (gchar *) "RunningAs",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "running-as",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_init_rdtimestamp =
{
  {
    -1,
    (gchar *) "InitRDTimestamp",
    (gchar *) "t",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "init-rdtimestamp",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_init_rdtimestamp_monotonic =
{
  {
    -1,
    (gchar *) "InitRDTimestampMonotonic",
    (gchar *) "t",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "init-rdtimestamp-monotonic",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_startup_timestamp =
{
  {
    -1,
    (gchar *) "StartupTimestamp",
    (gchar *) "t",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "startup-timestamp",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_startup_timestamp_monotonic =
{
  {
    -1,
    (gchar *) "StartupTimestampMonotonic",
    (gchar *) "t",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "startup-timestamp-monotonic",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_finish_timestamp =
{
  {
    -1,
    (gchar *) "FinishTimestamp",
    (gchar *) "t",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "finish-timestamp",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_finish_timestamp_monotonic =
{
  {
    -1,
    (gchar *) "FinishTimestampMonotonic",
    (gchar *) "t",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "finish-timestamp-monotonic",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_log_level =
{
  {
    -1,
    (gchar *) "LogLevel",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "log-level",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_log_target =
{
  {
    -1,
    (gchar *) "LogTarget",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "log-target",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_nnames =
{
  {
    -1,
    (gchar *) "NNames",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "nnames",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_njobs =
{
  {
    -1,
    (gchar *) "NJobs",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "njobs",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_ninstalled_jobs =
{
  {
    -1,
    (gchar *) "NInstalledJobs",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "ninstalled-jobs",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_nfailed_jobs =
{
  {
    -1,
    (gchar *) "NFailedJobs",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "nfailed-jobs",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_progress =
{
  {
    -1,
    (gchar *) "Progress",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "progress",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_environment =
{
  {
    -1,
    (gchar *) "Environment",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "environment",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_confirm_spawn =
{
  {
    -1,
    (gchar *) "ConfirmSpawn",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "confirm-spawn",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_show_status =
{
  {
    -1,
    (gchar *) "ShowStatus",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "show-status",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_unit_path =
{
  {
    -1,
    (gchar *) "UnitPath",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "unit-path",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_notify_socket =
{
  {
    -1,
    (gchar *) "NotifySocket",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "notify-socket",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_control_group_hierarchy =
{
  {
    -1,
    (gchar *) "ControlGroupHierarchy",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "control-group-hierarchy",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_mount_auto =
{
  {
    -1,
    (gchar *) "MountAuto",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "mount-auto",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_swap_auto =
{
  {
    -1,
    (gchar *) "SwapAuto",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "swap-auto",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_default_controllers =
{
  {
    -1,
    (gchar *) "DefaultControllers",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "default-controllers",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_default_standard_output =
{
  {
    -1,
    (gchar *) "DefaultStandardOutput",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "default-standard-output",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_default_standard_error =
{
  {
    -1,
    (gchar *) "DefaultStandardError",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "default-standard-error",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_sys_vconsole =
{
  {
    -1,
    (gchar *) "SysVConsole",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "sys-vconsole",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_sys_vinit_path =
{
  {
    -1,
    (gchar *) "SysVInitPath",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "sys-vinit-path",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _realm_dbus_systemd_manager_property_info_sys_vrcnd_path =
{
  {
    -1,
    (gchar *) "SysVRcndPath",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "sys-vrcnd-path",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _realm_dbus_systemd_manager_property_info_pointers[] =
{
  &_realm_dbus_systemd_manager_property_info_version,
  &_realm_dbus_systemd_manager_property_info_distribution,
  &_realm_dbus_systemd_manager_property_info_features,
  &_realm_dbus_systemd_manager_property_info_tainted,
  &_realm_dbus_systemd_manager_property_info_running_as,
  &_realm_dbus_systemd_manager_property_info_init_rdtimestamp,
  &_realm_dbus_systemd_manager_property_info_init_rdtimestamp_monotonic,
  &_realm_dbus_systemd_manager_property_info_startup_timestamp,
  &_realm_dbus_systemd_manager_property_info_startup_timestamp_monotonic,
  &_realm_dbus_systemd_manager_property_info_finish_timestamp,
  &_realm_dbus_systemd_manager_property_info_finish_timestamp_monotonic,
  &_realm_dbus_systemd_manager_property_info_log_level,
  &_realm_dbus_systemd_manager_property_info_log_target,
  &_realm_dbus_systemd_manager_property_info_nnames,
  &_realm_dbus_systemd_manager_property_info_njobs,
  &_realm_dbus_systemd_manager_property_info_ninstalled_jobs,
  &_realm_dbus_systemd_manager_property_info_nfailed_jobs,
  &_realm_dbus_systemd_manager_property_info_progress,
  &_realm_dbus_systemd_manager_property_info_environment,
  &_realm_dbus_systemd_manager_property_info_confirm_spawn,
  &_realm_dbus_systemd_manager_property_info_show_status,
  &_realm_dbus_systemd_manager_property_info_unit_path,
  &_realm_dbus_systemd_manager_property_info_notify_socket,
  &_realm_dbus_systemd_manager_property_info_control_group_hierarchy,
  &_realm_dbus_systemd_manager_property_info_mount_auto,
  &_realm_dbus_systemd_manager_property_info_swap_auto,
  &_realm_dbus_systemd_manager_property_info_default_controllers,
  &_realm_dbus_systemd_manager_property_info_default_standard_output,
  &_realm_dbus_systemd_manager_property_info_default_standard_error,
  &_realm_dbus_systemd_manager_property_info_sys_vconsole,
  &_realm_dbus_systemd_manager_property_info_sys_vinit_path,
  &_realm_dbus_systemd_manager_property_info_sys_vrcnd_path,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _realm_dbus_systemd_manager_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.systemd1.Manager",
    (GDBusMethodInfo **) &_realm_dbus_systemd_manager_method_info_pointers,
    (GDBusSignalInfo **) &_realm_dbus_systemd_manager_signal_info_pointers,
    (GDBusPropertyInfo **) &_realm_dbus_systemd_manager_property_info_pointers,
    NULL
  },
  "manager",
};


/**
 * realm_dbus_systemd_manager_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-systemd1-Manager.top_of_page">org.freedesktop.systemd1.Manager</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
realm_dbus_systemd_manager_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_realm_dbus_systemd_manager_interface_info;
}

/**
 * realm_dbus_systemd_manager_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #RealmDbusSystemdManager interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
realm_dbus_systemd_manager_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "version");
  g_object_class_override_property (klass, property_id_begin++, "distribution");
  g_object_class_override_property (klass, property_id_begin++, "features");
  g_object_class_override_property (klass, property_id_begin++, "tainted");
  g_object_class_override_property (klass, property_id_begin++, "running-as");
  g_object_class_override_property (klass, property_id_begin++, "init-rdtimestamp");
  g_object_class_override_property (klass, property_id_begin++, "init-rdtimestamp-monotonic");
  g_object_class_override_property (klass, property_id_begin++, "startup-timestamp");
  g_object_class_override_property (klass, property_id_begin++, "startup-timestamp-monotonic");
  g_object_class_override_property (klass, property_id_begin++, "finish-timestamp");
  g_object_class_override_property (klass, property_id_begin++, "finish-timestamp-monotonic");
  g_object_class_override_property (klass, property_id_begin++, "log-level");
  g_object_class_override_property (klass, property_id_begin++, "log-target");
  g_object_class_override_property (klass, property_id_begin++, "nnames");
  g_object_class_override_property (klass, property_id_begin++, "njobs");
  g_object_class_override_property (klass, property_id_begin++, "ninstalled-jobs");
  g_object_class_override_property (klass, property_id_begin++, "nfailed-jobs");
  g_object_class_override_property (klass, property_id_begin++, "progress");
  g_object_class_override_property (klass, property_id_begin++, "environment");
  g_object_class_override_property (klass, property_id_begin++, "confirm-spawn");
  g_object_class_override_property (klass, property_id_begin++, "show-status");
  g_object_class_override_property (klass, property_id_begin++, "unit-path");
  g_object_class_override_property (klass, property_id_begin++, "notify-socket");
  g_object_class_override_property (klass, property_id_begin++, "control-group-hierarchy");
  g_object_class_override_property (klass, property_id_begin++, "mount-auto");
  g_object_class_override_property (klass, property_id_begin++, "swap-auto");
  g_object_class_override_property (klass, property_id_begin++, "default-controllers");
  g_object_class_override_property (klass, property_id_begin++, "default-standard-output");
  g_object_class_override_property (klass, property_id_begin++, "default-standard-error");
  g_object_class_override_property (klass, property_id_begin++, "sys-vconsole");
  g_object_class_override_property (klass, property_id_begin++, "sys-vinit-path");
  g_object_class_override_property (klass, property_id_begin++, "sys-vrcnd-path");
  return property_id_begin - 1;
}



/**
 * RealmDbusSystemdManager:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-systemd1-Manager.top_of_page">org.freedesktop.systemd1.Manager</link>.
 */

/**
 * RealmDbusSystemdManagerIface:
 * @parent_iface: The parent interface.
 * @handle_clear_jobs: Handler for the #RealmDbusSystemdManager::handle-clear-jobs signal.
 * @handle_create_snapshot: Handler for the #RealmDbusSystemdManager::handle-create-snapshot signal.
 * @handle_disable_unit_files: Handler for the #RealmDbusSystemdManager::handle-disable-unit-files signal.
 * @handle_dump: Handler for the #RealmDbusSystemdManager::handle-dump signal.
 * @handle_enable_unit_files: Handler for the #RealmDbusSystemdManager::handle-enable-unit-files signal.
 * @handle_exit: Handler for the #RealmDbusSystemdManager::handle-exit signal.
 * @handle_get_job: Handler for the #RealmDbusSystemdManager::handle-get-job signal.
 * @handle_get_unit: Handler for the #RealmDbusSystemdManager::handle-get-unit signal.
 * @handle_get_unit_by_pid: Handler for the #RealmDbusSystemdManager::handle-get-unit-by-pid signal.
 * @handle_get_unit_file_state: Handler for the #RealmDbusSystemdManager::handle-get-unit-file-state signal.
 * @handle_halt: Handler for the #RealmDbusSystemdManager::handle-halt signal.
 * @handle_kexec: Handler for the #RealmDbusSystemdManager::handle-kexec signal.
 * @handle_kill_unit: Handler for the #RealmDbusSystemdManager::handle-kill-unit signal.
 * @handle_link_unit_files: Handler for the #RealmDbusSystemdManager::handle-link-unit-files signal.
 * @handle_list_jobs: Handler for the #RealmDbusSystemdManager::handle-list-jobs signal.
 * @handle_list_unit_files: Handler for the #RealmDbusSystemdManager::handle-list-unit-files signal.
 * @handle_list_units: Handler for the #RealmDbusSystemdManager::handle-list-units signal.
 * @handle_load_unit: Handler for the #RealmDbusSystemdManager::handle-load-unit signal.
 * @handle_mask_unit_files: Handler for the #RealmDbusSystemdManager::handle-mask-unit-files signal.
 * @handle_power_off: Handler for the #RealmDbusSystemdManager::handle-power-off signal.
 * @handle_preset_unit_files: Handler for the #RealmDbusSystemdManager::handle-preset-unit-files signal.
 * @handle_reboot: Handler for the #RealmDbusSystemdManager::handle-reboot signal.
 * @handle_reenable_unit_files: Handler for the #RealmDbusSystemdManager::handle-reenable-unit-files signal.
 * @handle_reexecute: Handler for the #RealmDbusSystemdManager::handle-reexecute signal.
 * @handle_reload: Handler for the #RealmDbusSystemdManager::handle-reload signal.
 * @handle_reload_or_restart_unit: Handler for the #RealmDbusSystemdManager::handle-reload-or-restart-unit signal.
 * @handle_reload_or_try_restart_unit: Handler for the #RealmDbusSystemdManager::handle-reload-or-try-restart-unit signal.
 * @handle_reload_unit: Handler for the #RealmDbusSystemdManager::handle-reload-unit signal.
 * @handle_reset_failed: Handler for the #RealmDbusSystemdManager::handle-reset-failed signal.
 * @handle_reset_failed_unit: Handler for the #RealmDbusSystemdManager::handle-reset-failed-unit signal.
 * @handle_restart_unit: Handler for the #RealmDbusSystemdManager::handle-restart-unit signal.
 * @handle_set_environment: Handler for the #RealmDbusSystemdManager::handle-set-environment signal.
 * @handle_start_unit: Handler for the #RealmDbusSystemdManager::handle-start-unit signal.
 * @handle_start_unit_replace: Handler for the #RealmDbusSystemdManager::handle-start-unit-replace signal.
 * @handle_stop_unit: Handler for the #RealmDbusSystemdManager::handle-stop-unit signal.
 * @handle_subscribe: Handler for the #RealmDbusSystemdManager::handle-subscribe signal.
 * @handle_try_restart_unit: Handler for the #RealmDbusSystemdManager::handle-try-restart-unit signal.
 * @handle_unmask_unit_files: Handler for the #RealmDbusSystemdManager::handle-unmask-unit-files signal.
 * @handle_unset_and_set_environment: Handler for the #RealmDbusSystemdManager::handle-unset-and-set-environment signal.
 * @handle_unset_environment: Handler for the #RealmDbusSystemdManager::handle-unset-environment signal.
 * @handle_unsubscribe: Handler for the #RealmDbusSystemdManager::handle-unsubscribe signal.
 * @get_confirm_spawn: Getter for the #RealmDbusSystemdManager:confirm-spawn property.
 * @get_control_group_hierarchy: Getter for the #RealmDbusSystemdManager:control-group-hierarchy property.
 * @get_default_controllers: Getter for the #RealmDbusSystemdManager:default-controllers property.
 * @get_default_standard_error: Getter for the #RealmDbusSystemdManager:default-standard-error property.
 * @get_default_standard_output: Getter for the #RealmDbusSystemdManager:default-standard-output property.
 * @get_distribution: Getter for the #RealmDbusSystemdManager:distribution property.
 * @get_environment: Getter for the #RealmDbusSystemdManager:environment property.
 * @get_features: Getter for the #RealmDbusSystemdManager:features property.
 * @get_finish_timestamp: Getter for the #RealmDbusSystemdManager:finish-timestamp property.
 * @get_finish_timestamp_monotonic: Getter for the #RealmDbusSystemdManager:finish-timestamp-monotonic property.
 * @get_init_rdtimestamp: Getter for the #RealmDbusSystemdManager:init-rdtimestamp property.
 * @get_init_rdtimestamp_monotonic: Getter for the #RealmDbusSystemdManager:init-rdtimestamp-monotonic property.
 * @get_log_level: Getter for the #RealmDbusSystemdManager:log-level property.
 * @get_log_target: Getter for the #RealmDbusSystemdManager:log-target property.
 * @get_mount_auto: Getter for the #RealmDbusSystemdManager:mount-auto property.
 * @get_nfailed_jobs: Getter for the #RealmDbusSystemdManager:nfailed-jobs property.
 * @get_ninstalled_jobs: Getter for the #RealmDbusSystemdManager:ninstalled-jobs property.
 * @get_njobs: Getter for the #RealmDbusSystemdManager:njobs property.
 * @get_nnames: Getter for the #RealmDbusSystemdManager:nnames property.
 * @get_notify_socket: Getter for the #RealmDbusSystemdManager:notify-socket property.
 * @get_progress: Getter for the #RealmDbusSystemdManager:progress property.
 * @get_running_as: Getter for the #RealmDbusSystemdManager:running-as property.
 * @get_show_status: Getter for the #RealmDbusSystemdManager:show-status property.
 * @get_startup_timestamp: Getter for the #RealmDbusSystemdManager:startup-timestamp property.
 * @get_startup_timestamp_monotonic: Getter for the #RealmDbusSystemdManager:startup-timestamp-monotonic property.
 * @get_swap_auto: Getter for the #RealmDbusSystemdManager:swap-auto property.
 * @get_sys_vconsole: Getter for the #RealmDbusSystemdManager:sys-vconsole property.
 * @get_sys_vinit_path: Getter for the #RealmDbusSystemdManager:sys-vinit-path property.
 * @get_sys_vrcnd_path: Getter for the #RealmDbusSystemdManager:sys-vrcnd-path property.
 * @get_tainted: Getter for the #RealmDbusSystemdManager:tainted property.
 * @get_unit_path: Getter for the #RealmDbusSystemdManager:unit-path property.
 * @get_version: Getter for the #RealmDbusSystemdManager:version property.
 * @job_new: Handler for the #RealmDbusSystemdManager::job-new signal.
 * @job_removed: Handler for the #RealmDbusSystemdManager::job-removed signal.
 * @startup_finished: Handler for the #RealmDbusSystemdManager::startup-finished signal.
 * @unit_files_changed: Handler for the #RealmDbusSystemdManager::unit-files-changed signal.
 * @unit_new: Handler for the #RealmDbusSystemdManager::unit-new signal.
 * @unit_removed: Handler for the #RealmDbusSystemdManager::unit-removed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-systemd1-Manager.top_of_page">org.freedesktop.systemd1.Manager</link>.
 */

static void
realm_dbus_systemd_manager_default_init (RealmDbusSystemdManagerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * RealmDbusSystemdManager::handle-get-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnit">GetUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_get_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_get_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-get-unit-by-pid:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_pid: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnitByPID">GetUnitByPID()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_get_unit_by_pid() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-unit-by-pid",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_get_unit_by_pid),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * RealmDbusSystemdManager::handle-load-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.LoadUnit">LoadUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_load_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-load-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_load_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-start-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StartUnit">StartUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_start_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_start_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-start-unit-replace:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_old_unit: Argument passed by remote caller.
   * @arg_new_unit: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StartUnitReplace">StartUnitReplace()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_start_unit_replace() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-unit-replace",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_start_unit_replace),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-stop-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StopUnit">StopUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_stop_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_stop_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-reload-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadUnit">ReloadUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reload_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reload-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reload_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-restart-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.RestartUnit">RestartUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_restart_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-restart-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_restart_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-try-restart-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.TryRestartUnit">TryRestartUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_try_restart_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-try-restart-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_try_restart_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-reload-or-restart-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadOrRestartUnit">ReloadOrRestartUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reload_or_restart_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reload-or-restart-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reload_or_restart_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-reload-or-try-restart-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadOrTryRestartUnit">ReloadOrTryRestartUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reload_or_try_restart_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reload-or-try-restart-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reload_or_try_restart_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-kill-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_who: Argument passed by remote caller.
   * @arg_mode: Argument passed by remote caller.
   * @arg_signal: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.KillUnit">KillUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_kill_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-kill-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_kill_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    5,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_INT);

  /**
   * RealmDbusSystemdManager::handle-reset-failed-unit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ResetFailedUnit">ResetFailedUnit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reset_failed_unit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reset-failed-unit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reset_failed_unit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-get-job:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetJob">GetJob()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_get_job() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-job",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_get_job),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * RealmDbusSystemdManager::handle-clear-jobs:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ClearJobs">ClearJobs()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_clear_jobs() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-clear-jobs",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_clear_jobs),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-reset-failed:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ResetFailed">ResetFailed()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reset_failed() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reset-failed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reset_failed),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-list-units:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListUnits">ListUnits()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_list_units() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-list-units",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_list_units),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-list-jobs:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListJobs">ListJobs()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_list_jobs() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-list-jobs",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_list_jobs),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-subscribe:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Subscribe">Subscribe()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_subscribe() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-subscribe",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_subscribe),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-unsubscribe:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Unsubscribe">Unsubscribe()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_unsubscribe() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-unsubscribe",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_unsubscribe),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-dump:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Dump">Dump()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_dump() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-dump",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_dump),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-create-snapshot:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_cleanup: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.CreateSnapshot">CreateSnapshot()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_create_snapshot() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-create-snapshot",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_create_snapshot),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_BOOLEAN);

  /**
   * RealmDbusSystemdManager::handle-reload:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reload">Reload()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reload() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reload",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reload),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-reexecute:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reexecute">Reexecute()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reexecute() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reexecute",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reexecute),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-exit:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Exit">Exit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_exit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-exit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_exit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-reboot:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reboot">Reboot()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reboot() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reboot",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reboot),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-power-off:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.PowerOff">PowerOff()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_power_off() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-power-off",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_power_off),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-halt:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Halt">Halt()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_halt() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-halt",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_halt),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-kexec:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.KExec">KExec()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_kexec() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-kexec",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_kexec),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-set-environment:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_names: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.SetEnvironment">SetEnvironment()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_set_environment() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-environment",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_set_environment),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV);

  /**
   * RealmDbusSystemdManager::handle-unset-environment:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_names: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnsetEnvironment">UnsetEnvironment()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_unset_environment() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-unset-environment",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_unset_environment),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV);

  /**
   * RealmDbusSystemdManager::handle-unset-and-set-environment:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_unset: Argument passed by remote caller.
   * @arg_set: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnsetAndSetEnvironment">UnsetAndSetEnvironment()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_unset_and_set_environment() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-unset-and-set-environment",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_unset_and_set_environment),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV, G_TYPE_STRV);

  /**
   * RealmDbusSystemdManager::handle-list-unit-files:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListUnitFiles">ListUnitFiles()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_list_unit_files() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-list-unit-files",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_list_unit_files),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * RealmDbusSystemdManager::handle-get-unit-file-state:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_file: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnitFileState">GetUnitFileState()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_get_unit_file_state() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-unit-file-state",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_get_unit_file_state),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::handle-enable-unit-files:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_files: Argument passed by remote caller.
   * @arg_runtime: Argument passed by remote caller.
   * @arg_force: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.EnableUnitFiles">EnableUnitFiles()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_enable_unit_files() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-enable-unit-files",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_enable_unit_files),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV, G_TYPE_BOOLEAN, G_TYPE_BOOLEAN);

  /**
   * RealmDbusSystemdManager::handle-disable-unit-files:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_files: Argument passed by remote caller.
   * @arg_runtime: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.DisableUnitFiles">DisableUnitFiles()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_disable_unit_files() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-disable-unit-files",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_disable_unit_files),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV, G_TYPE_BOOLEAN);

  /**
   * RealmDbusSystemdManager::handle-reenable-unit-files:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_files: Argument passed by remote caller.
   * @arg_runtime: Argument passed by remote caller.
   * @arg_force: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReenableUnitFiles">ReenableUnitFiles()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_reenable_unit_files() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reenable-unit-files",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_reenable_unit_files),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV, G_TYPE_BOOLEAN, G_TYPE_BOOLEAN);

  /**
   * RealmDbusSystemdManager::handle-link-unit-files:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_files: Argument passed by remote caller.
   * @arg_runtime: Argument passed by remote caller.
   * @arg_force: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.LinkUnitFiles">LinkUnitFiles()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_link_unit_files() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-link-unit-files",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_link_unit_files),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV, G_TYPE_BOOLEAN, G_TYPE_BOOLEAN);

  /**
   * RealmDbusSystemdManager::handle-preset-unit-files:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_files: Argument passed by remote caller.
   * @arg_runtime: Argument passed by remote caller.
   * @arg_force: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.PresetUnitFiles">PresetUnitFiles()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_preset_unit_files() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-preset-unit-files",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_preset_unit_files),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV, G_TYPE_BOOLEAN, G_TYPE_BOOLEAN);

  /**
   * RealmDbusSystemdManager::handle-mask-unit-files:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_files: Argument passed by remote caller.
   * @arg_runtime: Argument passed by remote caller.
   * @arg_force: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.MaskUnitFiles">MaskUnitFiles()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_mask_unit_files() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-mask-unit-files",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_mask_unit_files),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV, G_TYPE_BOOLEAN, G_TYPE_BOOLEAN);

  /**
   * RealmDbusSystemdManager::handle-unmask-unit-files:
   * @object: A #RealmDbusSystemdManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_files: Argument passed by remote caller.
   * @arg_runtime: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnmaskUnitFiles">UnmaskUnitFiles()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call realm_dbus_systemd_manager_complete_unmask_unit_files() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-unmask-unit-files",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, handle_unmask_unit_files),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * RealmDbusSystemdManager::unit-new:
   * @object: A #RealmDbusSystemdManager.
   * @arg_id: Argument.
   * @arg_unit: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.UnitNew">"UnitNew"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("unit-new",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, unit_new),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::unit-removed:
   * @object: A #RealmDbusSystemdManager.
   * @arg_id: Argument.
   * @arg_unit: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.UnitRemoved">"UnitRemoved"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("unit-removed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, unit_removed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::job-new:
   * @object: A #RealmDbusSystemdManager.
   * @arg_id: Argument.
   * @arg_job: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.JobNew">"JobNew"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("job-new",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, job_new),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_UINT, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::job-removed:
   * @object: A #RealmDbusSystemdManager.
   * @arg_id: Argument.
   * @arg_job: Argument.
   * @arg_result: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.JobRemoved">"JobRemoved"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("job-removed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, job_removed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    3, G_TYPE_UINT, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * RealmDbusSystemdManager::startup-finished:
   * @object: A #RealmDbusSystemdManager.
   * @arg_kernel: Argument.
   * @arg_initrd: Argument.
   * @arg_userspace: Argument.
   * @arg_total: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.StartupFinished">"StartupFinished"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("startup-finished",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, startup_finished),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    4, G_TYPE_UINT64, G_TYPE_UINT64, G_TYPE_UINT64, G_TYPE_UINT64);

  /**
   * RealmDbusSystemdManager::unit-files-changed:
   * @object: A #RealmDbusSystemdManager.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.UnitFilesChanged">"UnitFilesChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("unit-files-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (RealmDbusSystemdManagerIface, unit_files_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

  /* GObject properties for D-Bus properties: */
  /**
   * RealmDbusSystemdManager:version:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Version">"Version"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("version", "Version", "Version", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:distribution:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Distribution">"Distribution"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("distribution", "Distribution", "Distribution", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:features:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Features">"Features"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("features", "Features", "Features", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:tainted:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Tainted">"Tainted"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("tainted", "Tainted", "Tainted", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:running-as:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.RunningAs">"RunningAs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("running-as", "RunningAs", "RunningAs", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:init-rdtimestamp:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.InitRDTimestamp">"InitRDTimestamp"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint64 ("init-rdtimestamp", "InitRDTimestamp", "InitRDTimestamp", 0, G_MAXUINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:init-rdtimestamp-monotonic:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.InitRDTimestampMonotonic">"InitRDTimestampMonotonic"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint64 ("init-rdtimestamp-monotonic", "InitRDTimestampMonotonic", "InitRDTimestampMonotonic", 0, G_MAXUINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:startup-timestamp:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.StartupTimestamp">"StartupTimestamp"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint64 ("startup-timestamp", "StartupTimestamp", "StartupTimestamp", 0, G_MAXUINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:startup-timestamp-monotonic:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.StartupTimestampMonotonic">"StartupTimestampMonotonic"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint64 ("startup-timestamp-monotonic", "StartupTimestampMonotonic", "StartupTimestampMonotonic", 0, G_MAXUINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:finish-timestamp:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.FinishTimestamp">"FinishTimestamp"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint64 ("finish-timestamp", "FinishTimestamp", "FinishTimestamp", 0, G_MAXUINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:finish-timestamp-monotonic:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.FinishTimestampMonotonic">"FinishTimestampMonotonic"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint64 ("finish-timestamp-monotonic", "FinishTimestampMonotonic", "FinishTimestampMonotonic", 0, G_MAXUINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:log-level:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.LogLevel">"LogLevel"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("log-level", "LogLevel", "LogLevel", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:log-target:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.LogTarget">"LogTarget"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("log-target", "LogTarget", "LogTarget", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:nnames:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NNames">"NNames"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("nnames", "NNames", "NNames", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:njobs:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NJobs">"NJobs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("njobs", "NJobs", "NJobs", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:ninstalled-jobs:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NInstalledJobs">"NInstalledJobs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("ninstalled-jobs", "NInstalledJobs", "NInstalledJobs", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:nfailed-jobs:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NFailedJobs">"NFailedJobs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("nfailed-jobs", "NFailedJobs", "NFailedJobs", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:progress:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Progress">"Progress"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("progress", "Progress", "Progress", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:environment:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Environment">"Environment"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("environment", "Environment", "Environment", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:confirm-spawn:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ConfirmSpawn">"ConfirmSpawn"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("confirm-spawn", "ConfirmSpawn", "ConfirmSpawn", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:show-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ShowStatus">"ShowStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("show-status", "ShowStatus", "ShowStatus", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:unit-path:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.UnitPath">"UnitPath"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("unit-path", "UnitPath", "UnitPath", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:notify-socket:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NotifySocket">"NotifySocket"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("notify-socket", "NotifySocket", "NotifySocket", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:control-group-hierarchy:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ControlGroupHierarchy">"ControlGroupHierarchy"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("control-group-hierarchy", "ControlGroupHierarchy", "ControlGroupHierarchy", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:mount-auto:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.MountAuto">"MountAuto"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("mount-auto", "MountAuto", "MountAuto", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:swap-auto:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SwapAuto">"SwapAuto"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("swap-auto", "SwapAuto", "SwapAuto", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:default-controllers:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultControllers">"DefaultControllers"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("default-controllers", "DefaultControllers", "DefaultControllers", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:default-standard-output:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultStandardOutput">"DefaultStandardOutput"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("default-standard-output", "DefaultStandardOutput", "DefaultStandardOutput", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:default-standard-error:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultStandardError">"DefaultStandardError"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("default-standard-error", "DefaultStandardError", "DefaultStandardError", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:sys-vconsole:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVConsole">"SysVConsole"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("sys-vconsole", "SysVConsole", "SysVConsole", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:sys-vinit-path:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVInitPath">"SysVInitPath"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("sys-vinit-path", "SysVInitPath", "SysVInitPath", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * RealmDbusSystemdManager:sys-vrcnd-path:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVRcndPath">"SysVRcndPath"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("sys-vrcnd-path", "SysVRcndPath", "SysVRcndPath", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

typedef RealmDbusSystemdManagerIface RealmDbusSystemdManagerInterface;
G_DEFINE_INTERFACE (RealmDbusSystemdManager, realm_dbus_systemd_manager, G_TYPE_OBJECT);

/**
 * realm_dbus_systemd_manager_get_version: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Version">"Version"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_version() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_version (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_version (object);
}

/**
 * realm_dbus_systemd_manager_dup_version: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Version">"Version"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_version (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "version", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_version: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Version">"Version"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_version (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "version", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_distribution: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Distribution">"Distribution"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_distribution() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_distribution (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_distribution (object);
}

/**
 * realm_dbus_systemd_manager_dup_distribution: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Distribution">"Distribution"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_distribution (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "distribution", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_distribution: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Distribution">"Distribution"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_distribution (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "distribution", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_features: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Features">"Features"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_features() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_features (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_features (object);
}

/**
 * realm_dbus_systemd_manager_dup_features: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Features">"Features"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_features (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "features", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_features: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Features">"Features"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_features (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "features", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_tainted: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Tainted">"Tainted"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_tainted() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_tainted (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_tainted (object);
}

/**
 * realm_dbus_systemd_manager_dup_tainted: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Tainted">"Tainted"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_tainted (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "tainted", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_tainted: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Tainted">"Tainted"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_tainted (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "tainted", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_running_as: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.RunningAs">"RunningAs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_running_as() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_running_as (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_running_as (object);
}

/**
 * realm_dbus_systemd_manager_dup_running_as: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.RunningAs">"RunningAs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_running_as (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "running-as", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_running_as: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.RunningAs">"RunningAs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_running_as (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "running-as", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_init_rdtimestamp: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.InitRDTimestamp">"InitRDTimestamp"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint64 
realm_dbus_systemd_manager_get_init_rdtimestamp (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_init_rdtimestamp (object);
}

/**
 * realm_dbus_systemd_manager_set_init_rdtimestamp: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.InitRDTimestamp">"InitRDTimestamp"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_init_rdtimestamp (RealmDbusSystemdManager *object, guint64 value)
{
  g_object_set (G_OBJECT (object), "init-rdtimestamp", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_init_rdtimestamp_monotonic: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.InitRDTimestampMonotonic">"InitRDTimestampMonotonic"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint64 
realm_dbus_systemd_manager_get_init_rdtimestamp_monotonic (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_init_rdtimestamp_monotonic (object);
}

/**
 * realm_dbus_systemd_manager_set_init_rdtimestamp_monotonic: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.InitRDTimestampMonotonic">"InitRDTimestampMonotonic"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_init_rdtimestamp_monotonic (RealmDbusSystemdManager *object, guint64 value)
{
  g_object_set (G_OBJECT (object), "init-rdtimestamp-monotonic", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_startup_timestamp: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.StartupTimestamp">"StartupTimestamp"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint64 
realm_dbus_systemd_manager_get_startup_timestamp (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_startup_timestamp (object);
}

/**
 * realm_dbus_systemd_manager_set_startup_timestamp: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.StartupTimestamp">"StartupTimestamp"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_startup_timestamp (RealmDbusSystemdManager *object, guint64 value)
{
  g_object_set (G_OBJECT (object), "startup-timestamp", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_startup_timestamp_monotonic: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.StartupTimestampMonotonic">"StartupTimestampMonotonic"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint64 
realm_dbus_systemd_manager_get_startup_timestamp_monotonic (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_startup_timestamp_monotonic (object);
}

/**
 * realm_dbus_systemd_manager_set_startup_timestamp_monotonic: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.StartupTimestampMonotonic">"StartupTimestampMonotonic"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_startup_timestamp_monotonic (RealmDbusSystemdManager *object, guint64 value)
{
  g_object_set (G_OBJECT (object), "startup-timestamp-monotonic", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_finish_timestamp: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.FinishTimestamp">"FinishTimestamp"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint64 
realm_dbus_systemd_manager_get_finish_timestamp (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_finish_timestamp (object);
}

/**
 * realm_dbus_systemd_manager_set_finish_timestamp: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.FinishTimestamp">"FinishTimestamp"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_finish_timestamp (RealmDbusSystemdManager *object, guint64 value)
{
  g_object_set (G_OBJECT (object), "finish-timestamp", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_finish_timestamp_monotonic: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.FinishTimestampMonotonic">"FinishTimestampMonotonic"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint64 
realm_dbus_systemd_manager_get_finish_timestamp_monotonic (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_finish_timestamp_monotonic (object);
}

/**
 * realm_dbus_systemd_manager_set_finish_timestamp_monotonic: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.FinishTimestampMonotonic">"FinishTimestampMonotonic"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_finish_timestamp_monotonic (RealmDbusSystemdManager *object, guint64 value)
{
  g_object_set (G_OBJECT (object), "finish-timestamp-monotonic", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_log_level: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.LogLevel">"LogLevel"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_log_level() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_log_level (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_log_level (object);
}

/**
 * realm_dbus_systemd_manager_dup_log_level: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.LogLevel">"LogLevel"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_log_level (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "log-level", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_log_level: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.LogLevel">"LogLevel"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
realm_dbus_systemd_manager_set_log_level (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "log-level", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_log_target: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.LogTarget">"LogTarget"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_log_target() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_log_target (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_log_target (object);
}

/**
 * realm_dbus_systemd_manager_dup_log_target: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.LogTarget">"LogTarget"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_log_target (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "log-target", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_log_target: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.LogTarget">"LogTarget"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
realm_dbus_systemd_manager_set_log_target (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "log-target", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_nnames: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NNames">"NNames"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
realm_dbus_systemd_manager_get_nnames (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_nnames (object);
}

/**
 * realm_dbus_systemd_manager_set_nnames: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NNames">"NNames"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_nnames (RealmDbusSystemdManager *object, guint value)
{
  g_object_set (G_OBJECT (object), "nnames", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_njobs: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NJobs">"NJobs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
realm_dbus_systemd_manager_get_njobs (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_njobs (object);
}

/**
 * realm_dbus_systemd_manager_set_njobs: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NJobs">"NJobs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_njobs (RealmDbusSystemdManager *object, guint value)
{
  g_object_set (G_OBJECT (object), "njobs", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_ninstalled_jobs: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NInstalledJobs">"NInstalledJobs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
realm_dbus_systemd_manager_get_ninstalled_jobs (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_ninstalled_jobs (object);
}

/**
 * realm_dbus_systemd_manager_set_ninstalled_jobs: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NInstalledJobs">"NInstalledJobs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_ninstalled_jobs (RealmDbusSystemdManager *object, guint value)
{
  g_object_set (G_OBJECT (object), "ninstalled-jobs", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_nfailed_jobs: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NFailedJobs">"NFailedJobs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
realm_dbus_systemd_manager_get_nfailed_jobs (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_nfailed_jobs (object);
}

/**
 * realm_dbus_systemd_manager_set_nfailed_jobs: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NFailedJobs">"NFailedJobs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_nfailed_jobs (RealmDbusSystemdManager *object, guint value)
{
  g_object_set (G_OBJECT (object), "nfailed-jobs", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_progress: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Progress">"Progress"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble 
realm_dbus_systemd_manager_get_progress (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_progress (object);
}

/**
 * realm_dbus_systemd_manager_set_progress: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Progress">"Progress"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_progress (RealmDbusSystemdManager *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "progress", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_environment: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Environment">"Environment"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_environment() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
realm_dbus_systemd_manager_get_environment (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_environment (object);
}

/**
 * realm_dbus_systemd_manager_dup_environment: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Environment">"Environment"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
realm_dbus_systemd_manager_dup_environment (RealmDbusSystemdManager *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "environment", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_environment: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.Environment">"Environment"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_environment (RealmDbusSystemdManager *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "environment", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_confirm_spawn: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ConfirmSpawn">"ConfirmSpawn"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
realm_dbus_systemd_manager_get_confirm_spawn (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_confirm_spawn (object);
}

/**
 * realm_dbus_systemd_manager_set_confirm_spawn: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ConfirmSpawn">"ConfirmSpawn"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_confirm_spawn (RealmDbusSystemdManager *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "confirm-spawn", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_show_status: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ShowStatus">"ShowStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
realm_dbus_systemd_manager_get_show_status (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_show_status (object);
}

/**
 * realm_dbus_systemd_manager_set_show_status: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ShowStatus">"ShowStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_show_status (RealmDbusSystemdManager *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "show-status", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_unit_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.UnitPath">"UnitPath"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_unit_path() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
realm_dbus_systemd_manager_get_unit_path (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_unit_path (object);
}

/**
 * realm_dbus_systemd_manager_dup_unit_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.UnitPath">"UnitPath"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
realm_dbus_systemd_manager_dup_unit_path (RealmDbusSystemdManager *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "unit-path", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_unit_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.UnitPath">"UnitPath"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_unit_path (RealmDbusSystemdManager *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "unit-path", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_notify_socket: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NotifySocket">"NotifySocket"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_notify_socket() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_notify_socket (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_notify_socket (object);
}

/**
 * realm_dbus_systemd_manager_dup_notify_socket: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NotifySocket">"NotifySocket"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_notify_socket (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "notify-socket", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_notify_socket: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.NotifySocket">"NotifySocket"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_notify_socket (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "notify-socket", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_control_group_hierarchy: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ControlGroupHierarchy">"ControlGroupHierarchy"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_control_group_hierarchy() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_control_group_hierarchy (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_control_group_hierarchy (object);
}

/**
 * realm_dbus_systemd_manager_dup_control_group_hierarchy: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ControlGroupHierarchy">"ControlGroupHierarchy"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_control_group_hierarchy (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "control-group-hierarchy", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_control_group_hierarchy: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.ControlGroupHierarchy">"ControlGroupHierarchy"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_control_group_hierarchy (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "control-group-hierarchy", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_mount_auto: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.MountAuto">"MountAuto"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
realm_dbus_systemd_manager_get_mount_auto (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_mount_auto (object);
}

/**
 * realm_dbus_systemd_manager_set_mount_auto: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.MountAuto">"MountAuto"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_mount_auto (RealmDbusSystemdManager *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "mount-auto", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_swap_auto: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SwapAuto">"SwapAuto"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
realm_dbus_systemd_manager_get_swap_auto (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_swap_auto (object);
}

/**
 * realm_dbus_systemd_manager_set_swap_auto: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SwapAuto">"SwapAuto"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_swap_auto (RealmDbusSystemdManager *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "swap-auto", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_default_controllers: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultControllers">"DefaultControllers"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_default_controllers() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
realm_dbus_systemd_manager_get_default_controllers (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_default_controllers (object);
}

/**
 * realm_dbus_systemd_manager_dup_default_controllers: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultControllers">"DefaultControllers"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
realm_dbus_systemd_manager_dup_default_controllers (RealmDbusSystemdManager *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "default-controllers", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_default_controllers: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultControllers">"DefaultControllers"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_default_controllers (RealmDbusSystemdManager *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "default-controllers", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_default_standard_output: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultStandardOutput">"DefaultStandardOutput"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_default_standard_output() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_default_standard_output (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_default_standard_output (object);
}

/**
 * realm_dbus_systemd_manager_dup_default_standard_output: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultStandardOutput">"DefaultStandardOutput"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_default_standard_output (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "default-standard-output", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_default_standard_output: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultStandardOutput">"DefaultStandardOutput"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_default_standard_output (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "default-standard-output", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_default_standard_error: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultStandardError">"DefaultStandardError"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_default_standard_error() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
realm_dbus_systemd_manager_get_default_standard_error (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_default_standard_error (object);
}

/**
 * realm_dbus_systemd_manager_dup_default_standard_error: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultStandardError">"DefaultStandardError"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
realm_dbus_systemd_manager_dup_default_standard_error (RealmDbusSystemdManager *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "default-standard-error", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_default_standard_error: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.DefaultStandardError">"DefaultStandardError"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_default_standard_error (RealmDbusSystemdManager *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "default-standard-error", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_sys_vconsole: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVConsole">"SysVConsole"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
realm_dbus_systemd_manager_get_sys_vconsole (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_sys_vconsole (object);
}

/**
 * realm_dbus_systemd_manager_set_sys_vconsole: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVConsole">"SysVConsole"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_sys_vconsole (RealmDbusSystemdManager *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "sys-vconsole", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_sys_vinit_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVInitPath">"SysVInitPath"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_sys_vinit_path() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
realm_dbus_systemd_manager_get_sys_vinit_path (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_sys_vinit_path (object);
}

/**
 * realm_dbus_systemd_manager_dup_sys_vinit_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVInitPath">"SysVInitPath"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
realm_dbus_systemd_manager_dup_sys_vinit_path (RealmDbusSystemdManager *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "sys-vinit-path", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_sys_vinit_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVInitPath">"SysVInitPath"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_sys_vinit_path (RealmDbusSystemdManager *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "sys-vinit-path", value, NULL);
}

/**
 * realm_dbus_systemd_manager_get_sys_vrcnd_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVRcndPath">"SysVRcndPath"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use realm_dbus_systemd_manager_dup_sys_vrcnd_path() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
realm_dbus_systemd_manager_get_sys_vrcnd_path (RealmDbusSystemdManager *object)
{
  return REALM_DBUS_SYSTEMD_MANAGER_GET_IFACE (object)->get_sys_vrcnd_path (object);
}

/**
 * realm_dbus_systemd_manager_dup_sys_vrcnd_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVRcndPath">"SysVRcndPath"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
realm_dbus_systemd_manager_dup_sys_vrcnd_path (RealmDbusSystemdManager *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "sys-vrcnd-path", &value, NULL);
  return value;
}

/**
 * realm_dbus_systemd_manager_set_sys_vrcnd_path: (skip)
 * @object: A #RealmDbusSystemdManager.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-systemd1-Manager.SysVRcndPath">"SysVRcndPath"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
realm_dbus_systemd_manager_set_sys_vrcnd_path (RealmDbusSystemdManager *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "sys-vrcnd-path", value, NULL);
}

/**
 * realm_dbus_systemd_manager_emit_unit_new:
 * @object: A #RealmDbusSystemdManager.
 * @arg_id: Argument to pass with the signal.
 * @arg_unit: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.UnitNew">"UnitNew"</link> D-Bus signal.
 */
void
realm_dbus_systemd_manager_emit_unit_new (
    RealmDbusSystemdManager *object,
    const gchar *arg_id,
    const gchar *arg_unit)
{
  g_signal_emit_by_name (object, "unit-new", arg_id, arg_unit);
}

/**
 * realm_dbus_systemd_manager_emit_unit_removed:
 * @object: A #RealmDbusSystemdManager.
 * @arg_id: Argument to pass with the signal.
 * @arg_unit: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.UnitRemoved">"UnitRemoved"</link> D-Bus signal.
 */
void
realm_dbus_systemd_manager_emit_unit_removed (
    RealmDbusSystemdManager *object,
    const gchar *arg_id,
    const gchar *arg_unit)
{
  g_signal_emit_by_name (object, "unit-removed", arg_id, arg_unit);
}

/**
 * realm_dbus_systemd_manager_emit_job_new:
 * @object: A #RealmDbusSystemdManager.
 * @arg_id: Argument to pass with the signal.
 * @arg_job: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.JobNew">"JobNew"</link> D-Bus signal.
 */
void
realm_dbus_systemd_manager_emit_job_new (
    RealmDbusSystemdManager *object,
    guint arg_id,
    const gchar *arg_job)
{
  g_signal_emit_by_name (object, "job-new", arg_id, arg_job);
}

/**
 * realm_dbus_systemd_manager_emit_job_removed:
 * @object: A #RealmDbusSystemdManager.
 * @arg_id: Argument to pass with the signal.
 * @arg_job: Argument to pass with the signal.
 * @arg_result: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.JobRemoved">"JobRemoved"</link> D-Bus signal.
 */
void
realm_dbus_systemd_manager_emit_job_removed (
    RealmDbusSystemdManager *object,
    guint arg_id,
    const gchar *arg_job,
    const gchar *arg_result)
{
  g_signal_emit_by_name (object, "job-removed", arg_id, arg_job, arg_result);
}

/**
 * realm_dbus_systemd_manager_emit_startup_finished:
 * @object: A #RealmDbusSystemdManager.
 * @arg_kernel: Argument to pass with the signal.
 * @arg_initrd: Argument to pass with the signal.
 * @arg_userspace: Argument to pass with the signal.
 * @arg_total: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.StartupFinished">"StartupFinished"</link> D-Bus signal.
 */
void
realm_dbus_systemd_manager_emit_startup_finished (
    RealmDbusSystemdManager *object,
    guint64 arg_kernel,
    guint64 arg_initrd,
    guint64 arg_userspace,
    guint64 arg_total)
{
  g_signal_emit_by_name (object, "startup-finished", arg_kernel, arg_initrd, arg_userspace, arg_total);
}

/**
 * realm_dbus_systemd_manager_emit_unit_files_changed:
 * @object: A #RealmDbusSystemdManager.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-systemd1-Manager.UnitFilesChanged">"UnitFilesChanged"</link> D-Bus signal.
 */
void
realm_dbus_systemd_manager_emit_unit_files_changed (
    RealmDbusSystemdManager *object)
{
  g_signal_emit_by_name (object, "unit-files-changed");
}

/**
 * realm_dbus_systemd_manager_call_get_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnit">GetUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_get_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_get_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_get_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetUnit",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_get_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_unit: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_get_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_get_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_get_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_unit,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_unit);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_get_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @out_unit: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnit">GetUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_get_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_get_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    gchar **out_unit,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetUnit",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_unit);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_get_unit_by_pid:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_pid: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnitByPID">GetUnitByPID()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_get_unit_by_pid_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_get_unit_by_pid_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_get_unit_by_pid (
    RealmDbusSystemdManager *proxy,
    guint arg_pid,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetUnitByPID",
    g_variant_new ("(u)",
                   arg_pid),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_get_unit_by_pid_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_unit: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_get_unit_by_pid().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_get_unit_by_pid().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_get_unit_by_pid_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_unit,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_unit);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_get_unit_by_pid_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_pid: Argument to pass with the method invocation.
 * @out_unit: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnitByPID">GetUnitByPID()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_get_unit_by_pid() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_get_unit_by_pid_sync (
    RealmDbusSystemdManager *proxy,
    guint arg_pid,
    gchar **out_unit,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetUnitByPID",
    g_variant_new ("(u)",
                   arg_pid),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_unit);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_load_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.LoadUnit">LoadUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_load_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_load_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_load_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "LoadUnit",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_load_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_unit: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_load_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_load_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_load_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_unit,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_unit);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_load_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @out_unit: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.LoadUnit">LoadUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_load_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_load_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    gchar **out_unit,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "LoadUnit",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_unit);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_start_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StartUnit">StartUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_start_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_start_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_start_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_start_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_start_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_start_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_start_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_start_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StartUnit">StartUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_start_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_start_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_start_unit_replace:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_old_unit: Argument to pass with the method invocation.
 * @arg_new_unit: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StartUnitReplace">StartUnitReplace()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_start_unit_replace_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_start_unit_replace_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_start_unit_replace (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_old_unit,
    const gchar *arg_new_unit,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StartUnitReplace",
    g_variant_new ("(sss)",
                   arg_old_unit,
                   arg_new_unit,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_start_unit_replace_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_start_unit_replace().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_start_unit_replace().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_start_unit_replace_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_start_unit_replace_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_old_unit: Argument to pass with the method invocation.
 * @arg_new_unit: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StartUnitReplace">StartUnitReplace()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_start_unit_replace() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_start_unit_replace_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_old_unit,
    const gchar *arg_new_unit,
    const gchar *arg_mode,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StartUnitReplace",
    g_variant_new ("(sss)",
                   arg_old_unit,
                   arg_new_unit,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_stop_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StopUnit">StopUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_stop_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_stop_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_stop_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StopUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_stop_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_stop_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_stop_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_stop_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_stop_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StopUnit">StopUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_stop_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_stop_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StopUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reload_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadUnit">ReloadUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reload_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reload_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reload_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ReloadUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reload_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reload_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reload_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reload_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reload_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadUnit">ReloadUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reload_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reload_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ReloadUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_restart_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.RestartUnit">RestartUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_restart_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_restart_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_restart_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RestartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_restart_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_restart_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_restart_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_restart_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_restart_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.RestartUnit">RestartUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_restart_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_restart_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RestartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_try_restart_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.TryRestartUnit">TryRestartUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_try_restart_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_try_restart_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_try_restart_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "TryRestartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_try_restart_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_try_restart_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_try_restart_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_try_restart_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_try_restart_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.TryRestartUnit">TryRestartUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_try_restart_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_try_restart_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "TryRestartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reload_or_restart_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadOrRestartUnit">ReloadOrRestartUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reload_or_restart_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reload_or_restart_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reload_or_restart_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ReloadOrRestartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reload_or_restart_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reload_or_restart_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reload_or_restart_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reload_or_restart_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reload_or_restart_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadOrRestartUnit">ReloadOrRestartUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reload_or_restart_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reload_or_restart_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ReloadOrRestartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reload_or_try_restart_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadOrTryRestartUnit">ReloadOrTryRestartUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reload_or_try_restart_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reload_or_try_restart_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reload_or_try_restart_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ReloadOrTryRestartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reload_or_try_restart_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reload_or_try_restart_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reload_or_try_restart_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reload_or_try_restart_unit_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reload_or_try_restart_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadOrTryRestartUnit">ReloadOrTryRestartUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reload_or_try_restart_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reload_or_try_restart_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_mode,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ReloadOrTryRestartUnit",
    g_variant_new ("(ss)",
                   arg_name,
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_kill_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_who: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @arg_signal: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.KillUnit">KillUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_kill_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_kill_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_kill_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_who,
    const gchar *arg_mode,
    gint arg_signal,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "KillUnit",
    g_variant_new ("(sssi)",
                   arg_name,
                   arg_who,
                   arg_mode,
                   arg_signal),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_kill_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_kill_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_kill_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_kill_unit_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_kill_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_who: Argument to pass with the method invocation.
 * @arg_mode: Argument to pass with the method invocation.
 * @arg_signal: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.KillUnit">KillUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_kill_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_kill_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    const gchar *arg_who,
    const gchar *arg_mode,
    gint arg_signal,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "KillUnit",
    g_variant_new ("(sssi)",
                   arg_name,
                   arg_who,
                   arg_mode,
                   arg_signal),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reset_failed_unit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ResetFailedUnit">ResetFailedUnit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reset_failed_unit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reset_failed_unit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reset_failed_unit (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ResetFailedUnit",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reset_failed_unit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reset_failed_unit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reset_failed_unit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reset_failed_unit_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reset_failed_unit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ResetFailedUnit">ResetFailedUnit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reset_failed_unit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reset_failed_unit_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ResetFailedUnit",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_get_job:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_id: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetJob">GetJob()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_get_job_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_get_job_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_get_job (
    RealmDbusSystemdManager *proxy,
    guint arg_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetJob",
    g_variant_new ("(u)",
                   arg_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_get_job_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_get_job().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_get_job().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_get_job_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_job,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_get_job_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_id: Argument to pass with the method invocation.
 * @out_job: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetJob">GetJob()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_get_job() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_get_job_sync (
    RealmDbusSystemdManager *proxy,
    guint arg_id,
    gchar **out_job,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetJob",
    g_variant_new ("(u)",
                   arg_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_job);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_clear_jobs:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ClearJobs">ClearJobs()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_clear_jobs_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_clear_jobs_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_clear_jobs (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ClearJobs",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_clear_jobs_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_clear_jobs().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_clear_jobs().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_clear_jobs_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_clear_jobs_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ClearJobs">ClearJobs()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_clear_jobs() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_clear_jobs_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ClearJobs",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reset_failed:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ResetFailed">ResetFailed()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reset_failed_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reset_failed_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reset_failed (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ResetFailed",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reset_failed_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reset_failed().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reset_failed().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reset_failed_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reset_failed_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ResetFailed">ResetFailed()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reset_failed() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reset_failed_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ResetFailed",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_list_units:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListUnits">ListUnits()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_list_units_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_list_units_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_list_units (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ListUnits",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_list_units_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_units: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_list_units().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_list_units().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_list_units_finish (
    RealmDbusSystemdManager *proxy,
    GVariant **out_units,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ssssssouso))",
                 out_units);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_list_units_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_units: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListUnits">ListUnits()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_list_units() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_list_units_sync (
    RealmDbusSystemdManager *proxy,
    GVariant **out_units,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ListUnits",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ssssssouso))",
                 out_units);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_list_jobs:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListJobs">ListJobs()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_list_jobs_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_list_jobs_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_list_jobs (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ListJobs",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_list_jobs_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_jobs: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_list_jobs().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_list_jobs().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_list_jobs_finish (
    RealmDbusSystemdManager *proxy,
    GVariant **out_jobs,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(usssoo))",
                 out_jobs);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_list_jobs_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_jobs: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListJobs">ListJobs()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_list_jobs() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_list_jobs_sync (
    RealmDbusSystemdManager *proxy,
    GVariant **out_jobs,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ListJobs",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(usssoo))",
                 out_jobs);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_subscribe:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Subscribe">Subscribe()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_subscribe_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_subscribe_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_subscribe (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Subscribe",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_subscribe_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_subscribe().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_subscribe().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_subscribe_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_subscribe_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Subscribe">Subscribe()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_subscribe() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_subscribe_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Subscribe",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_unsubscribe:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Unsubscribe">Unsubscribe()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_unsubscribe_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_unsubscribe_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_unsubscribe (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Unsubscribe",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_unsubscribe_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_unsubscribe().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_unsubscribe().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_unsubscribe_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_unsubscribe_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Unsubscribe">Unsubscribe()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_unsubscribe() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_unsubscribe_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Unsubscribe",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_dump:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Dump">Dump()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_dump_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_dump_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_dump (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Dump",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_dump_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_dump().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_dump().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_dump_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_dump_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Dump">Dump()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_dump() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_dump_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Dump",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_create_snapshot:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_cleanup: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.CreateSnapshot">CreateSnapshot()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_create_snapshot_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_create_snapshot_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_create_snapshot (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    gboolean arg_cleanup,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "CreateSnapshot",
    g_variant_new ("(sb)",
                   arg_name,
                   arg_cleanup),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_create_snapshot_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_unit: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_create_snapshot().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_create_snapshot().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_create_snapshot_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_unit,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_unit);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_create_snapshot_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_cleanup: Argument to pass with the method invocation.
 * @out_unit: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.CreateSnapshot">CreateSnapshot()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_create_snapshot() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_create_snapshot_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_name,
    gboolean arg_cleanup,
    gchar **out_unit,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "CreateSnapshot",
    g_variant_new ("(sb)",
                   arg_name,
                   arg_cleanup),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_unit);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reload:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reload">Reload()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reload_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reload_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reload (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Reload",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reload_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reload().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reload().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reload_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reload_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reload">Reload()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reload() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reload_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Reload",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reexecute:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reexecute">Reexecute()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reexecute_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reexecute_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reexecute (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Reexecute",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reexecute_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reexecute().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reexecute().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reexecute_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reexecute_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reexecute">Reexecute()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reexecute() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reexecute_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Reexecute",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_exit:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Exit">Exit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_exit_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_exit_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_exit (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Exit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_exit_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_exit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_exit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_exit_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_exit_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Exit">Exit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_exit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_exit_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Exit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reboot:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reboot">Reboot()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reboot_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reboot_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reboot (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Reboot",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reboot_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reboot().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reboot().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reboot_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reboot_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reboot">Reboot()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reboot() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reboot_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Reboot",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_power_off:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.PowerOff">PowerOff()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_power_off_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_power_off_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_power_off (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "PowerOff",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_power_off_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_power_off().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_power_off().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_power_off_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_power_off_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.PowerOff">PowerOff()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_power_off() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_power_off_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "PowerOff",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_halt:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Halt">Halt()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_halt_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_halt_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_halt (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Halt",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_halt_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_halt().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_halt().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_halt_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_halt_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Halt">Halt()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_halt() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_halt_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Halt",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_kexec:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.KExec">KExec()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_kexec_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_kexec_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_kexec (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "KExec",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_kexec_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_kexec().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_kexec().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_kexec_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_kexec_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.KExec">KExec()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_kexec() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_kexec_sync (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "KExec",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_set_environment:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_names: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.SetEnvironment">SetEnvironment()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_set_environment_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_set_environment_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_set_environment (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_names,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetEnvironment",
    g_variant_new ("(^as)",
                   arg_names),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_set_environment_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_set_environment().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_set_environment().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_set_environment_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_set_environment_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_names: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.SetEnvironment">SetEnvironment()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_set_environment() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_set_environment_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_names,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetEnvironment",
    g_variant_new ("(^as)",
                   arg_names),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_unset_environment:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_names: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnsetEnvironment">UnsetEnvironment()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_unset_environment_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_unset_environment_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_unset_environment (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_names,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "UnsetEnvironment",
    g_variant_new ("(^as)",
                   arg_names),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_unset_environment_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_unset_environment().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_unset_environment().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_unset_environment_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_unset_environment_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_names: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnsetEnvironment">UnsetEnvironment()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_unset_environment() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_unset_environment_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_names,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "UnsetEnvironment",
    g_variant_new ("(^as)",
                   arg_names),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_unset_and_set_environment:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_unset: Argument to pass with the method invocation.
 * @arg_set: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnsetAndSetEnvironment">UnsetAndSetEnvironment()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_unset_and_set_environment_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_unset_and_set_environment_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_unset_and_set_environment (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_unset,
    const gchar *const *arg_set,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "UnsetAndSetEnvironment",
    g_variant_new ("(^as^as)",
                   arg_unset,
                   arg_set),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_unset_and_set_environment_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_unset_and_set_environment().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_unset_and_set_environment().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_unset_and_set_environment_finish (
    RealmDbusSystemdManager *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_unset_and_set_environment_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_unset: Argument to pass with the method invocation.
 * @arg_set: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnsetAndSetEnvironment">UnsetAndSetEnvironment()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_unset_and_set_environment() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_unset_and_set_environment_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_unset,
    const gchar *const *arg_set,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "UnsetAndSetEnvironment",
    g_variant_new ("(^as^as)",
                   arg_unset,
                   arg_set),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_list_unit_files:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListUnitFiles">ListUnitFiles()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_list_unit_files_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_list_unit_files_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_list_unit_files (
    RealmDbusSystemdManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ListUnitFiles",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_list_unit_files_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_list_unit_files().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_list_unit_files().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_list_unit_files_finish (
    RealmDbusSystemdManager *proxy,
    GVariant **out_changes,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_list_unit_files_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListUnitFiles">ListUnitFiles()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_list_unit_files() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_list_unit_files_sync (
    RealmDbusSystemdManager *proxy,
    GVariant **out_changes,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ListUnitFiles",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_get_unit_file_state:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_file: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnitFileState">GetUnitFileState()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_get_unit_file_state_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_get_unit_file_state_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_get_unit_file_state (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_file,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetUnitFileState",
    g_variant_new ("(s)",
                   arg_file),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_get_unit_file_state_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_state: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_get_unit_file_state().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_get_unit_file_state().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_get_unit_file_state_finish (
    RealmDbusSystemdManager *proxy,
    gchar **out_state,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_state);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_get_unit_file_state_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_file: Argument to pass with the method invocation.
 * @out_state: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnitFileState">GetUnitFileState()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_get_unit_file_state() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_get_unit_file_state_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *arg_file,
    gchar **out_state,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetUnitFileState",
    g_variant_new ("(s)",
                   arg_file),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_state);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_enable_unit_files:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.EnableUnitFiles">EnableUnitFiles()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_enable_unit_files_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_enable_unit_files_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_enable_unit_files (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "EnableUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_enable_unit_files_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_carries_install_info: (out): Return location for return parameter or %NULL to ignore.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_enable_unit_files().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_enable_unit_files().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_enable_unit_files_finish (
    RealmDbusSystemdManager *proxy,
    gboolean *out_carries_install_info,
    GVariant **out_changes,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b@a(sss))",
                 out_carries_install_info,
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_enable_unit_files_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @out_carries_install_info: (out): Return location for return parameter or %NULL to ignore.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.EnableUnitFiles">EnableUnitFiles()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_enable_unit_files() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_enable_unit_files_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    gboolean *out_carries_install_info,
    GVariant **out_changes,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "EnableUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b@a(sss))",
                 out_carries_install_info,
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_disable_unit_files:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.DisableUnitFiles">DisableUnitFiles()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_disable_unit_files_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_disable_unit_files_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_disable_unit_files (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "DisableUnitFiles",
    g_variant_new ("(^asb)",
                   arg_files,
                   arg_runtime),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_disable_unit_files_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_disable_unit_files().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_disable_unit_files().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_disable_unit_files_finish (
    RealmDbusSystemdManager *proxy,
    GVariant **out_changes,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(sss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_disable_unit_files_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.DisableUnitFiles">DisableUnitFiles()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_disable_unit_files() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_disable_unit_files_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    GVariant **out_changes,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "DisableUnitFiles",
    g_variant_new ("(^asb)",
                   arg_files,
                   arg_runtime),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(sss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reenable_unit_files:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReenableUnitFiles">ReenableUnitFiles()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_reenable_unit_files_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_reenable_unit_files_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_reenable_unit_files (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ReenableUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_reenable_unit_files_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_carries_install_info: (out): Return location for return parameter or %NULL to ignore.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_reenable_unit_files().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_reenable_unit_files().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reenable_unit_files_finish (
    RealmDbusSystemdManager *proxy,
    gboolean *out_carries_install_info,
    GVariant **out_changes,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b@a(sss))",
                 out_carries_install_info,
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_reenable_unit_files_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @out_carries_install_info: (out): Return location for return parameter or %NULL to ignore.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReenableUnitFiles">ReenableUnitFiles()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_reenable_unit_files() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_reenable_unit_files_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    gboolean *out_carries_install_info,
    GVariant **out_changes,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ReenableUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b@a(sss))",
                 out_carries_install_info,
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_link_unit_files:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.LinkUnitFiles">LinkUnitFiles()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_link_unit_files_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_link_unit_files_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_link_unit_files (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "LinkUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_link_unit_files_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_link_unit_files().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_link_unit_files().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_link_unit_files_finish (
    RealmDbusSystemdManager *proxy,
    GVariant **out_changes,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(sss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_link_unit_files_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.LinkUnitFiles">LinkUnitFiles()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_link_unit_files() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_link_unit_files_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    GVariant **out_changes,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "LinkUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(sss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_preset_unit_files:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.PresetUnitFiles">PresetUnitFiles()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_preset_unit_files_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_preset_unit_files_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_preset_unit_files (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "PresetUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_preset_unit_files_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_carries_install_info: (out): Return location for return parameter or %NULL to ignore.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_preset_unit_files().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_preset_unit_files().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_preset_unit_files_finish (
    RealmDbusSystemdManager *proxy,
    gboolean *out_carries_install_info,
    GVariant **out_changes,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b@a(sss))",
                 out_carries_install_info,
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_preset_unit_files_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @out_carries_install_info: (out): Return location for return parameter or %NULL to ignore.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.PresetUnitFiles">PresetUnitFiles()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_preset_unit_files() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_preset_unit_files_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    gboolean *out_carries_install_info,
    GVariant **out_changes,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "PresetUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b@a(sss))",
                 out_carries_install_info,
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_mask_unit_files:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.MaskUnitFiles">MaskUnitFiles()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_mask_unit_files_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_mask_unit_files_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_mask_unit_files (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "MaskUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_mask_unit_files_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_mask_unit_files().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_mask_unit_files().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_mask_unit_files_finish (
    RealmDbusSystemdManager *proxy,
    GVariant **out_changes,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(sss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_mask_unit_files_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @arg_force: Argument to pass with the method invocation.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.MaskUnitFiles">MaskUnitFiles()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_mask_unit_files() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_mask_unit_files_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    gboolean arg_force,
    GVariant **out_changes,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "MaskUnitFiles",
    g_variant_new ("(^asbb)",
                   arg_files,
                   arg_runtime,
                   arg_force),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(sss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_unmask_unit_files:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnmaskUnitFiles">UnmaskUnitFiles()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_call_unmask_unit_files_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_call_unmask_unit_files_sync() for the synchronous, blocking version of this method.
 */
void
realm_dbus_systemd_manager_call_unmask_unit_files (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "UnmaskUnitFiles",
    g_variant_new ("(^asb)",
                   arg_files,
                   arg_runtime),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * realm_dbus_systemd_manager_call_unmask_unit_files_finish:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_call_unmask_unit_files().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with realm_dbus_systemd_manager_call_unmask_unit_files().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_unmask_unit_files_finish (
    RealmDbusSystemdManager *proxy,
    GVariant **out_changes,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(sss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_call_unmask_unit_files_sync:
 * @proxy: A #RealmDbusSystemdManagerProxy.
 * @arg_files: Argument to pass with the method invocation.
 * @arg_runtime: Argument to pass with the method invocation.
 * @out_changes: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnmaskUnitFiles">UnmaskUnitFiles()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_call_unmask_unit_files() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
realm_dbus_systemd_manager_call_unmask_unit_files_sync (
    RealmDbusSystemdManager *proxy,
    const gchar *const *arg_files,
    gboolean arg_runtime,
    GVariant **out_changes,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "UnmaskUnitFiles",
    g_variant_new ("(^asb)",
                   arg_files,
                   arg_runtime),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(sss))",
                 out_changes);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * realm_dbus_systemd_manager_complete_get_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @unit: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnit">GetUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_get_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *unit)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   unit));
}

/**
 * realm_dbus_systemd_manager_complete_get_unit_by_pid:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @unit: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnitByPID">GetUnitByPID()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_get_unit_by_pid (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *unit)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   unit));
}

/**
 * realm_dbus_systemd_manager_complete_load_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @unit: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.LoadUnit">LoadUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_load_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *unit)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   unit));
}

/**
 * realm_dbus_systemd_manager_complete_start_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StartUnit">StartUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_start_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_start_unit_replace:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StartUnitReplace">StartUnitReplace()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_start_unit_replace (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_stop_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.StopUnit">StopUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_stop_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_reload_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadUnit">ReloadUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reload_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_restart_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.RestartUnit">RestartUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_restart_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_try_restart_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.TryRestartUnit">TryRestartUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_try_restart_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_reload_or_restart_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadOrRestartUnit">ReloadOrRestartUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reload_or_restart_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_reload_or_try_restart_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReloadOrTryRestartUnit">ReloadOrTryRestartUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reload_or_try_restart_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_kill_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.KillUnit">KillUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_kill_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_reset_failed_unit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ResetFailedUnit">ResetFailedUnit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reset_failed_unit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_get_job:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @job: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetJob">GetJob()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_get_job (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *job)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   job));
}

/**
 * realm_dbus_systemd_manager_complete_clear_jobs:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ClearJobs">ClearJobs()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_clear_jobs (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_reset_failed:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ResetFailed">ResetFailed()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reset_failed (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_list_units:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @units: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListUnits">ListUnits()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_list_units (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *units)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(ssssssouso))",
                   units));
}

/**
 * realm_dbus_systemd_manager_complete_list_jobs:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @jobs: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListJobs">ListJobs()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_list_jobs (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *jobs)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(usssoo))",
                   jobs));
}

/**
 * realm_dbus_systemd_manager_complete_subscribe:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Subscribe">Subscribe()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_subscribe (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_unsubscribe:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Unsubscribe">Unsubscribe()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_unsubscribe (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_dump:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Dump">Dump()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_dump (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_create_snapshot:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @unit: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.CreateSnapshot">CreateSnapshot()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_create_snapshot (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *unit)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   unit));
}

/**
 * realm_dbus_systemd_manager_complete_reload:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reload">Reload()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reload (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_reexecute:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reexecute">Reexecute()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reexecute (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_exit:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Exit">Exit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_exit (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_reboot:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Reboot">Reboot()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reboot (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_power_off:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.PowerOff">PowerOff()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_power_off (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_halt:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.Halt">Halt()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_halt (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_kexec:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.KExec">KExec()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_kexec (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_set_environment:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.SetEnvironment">SetEnvironment()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_set_environment (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_unset_environment:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnsetEnvironment">UnsetEnvironment()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_unset_environment (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_unset_and_set_environment:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnsetAndSetEnvironment">UnsetAndSetEnvironment()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_unset_and_set_environment (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * realm_dbus_systemd_manager_complete_list_unit_files:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @changes: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ListUnitFiles">ListUnitFiles()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_list_unit_files (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *changes)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(ss))",
                   changes));
}

/**
 * realm_dbus_systemd_manager_complete_get_unit_file_state:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @state: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.GetUnitFileState">GetUnitFileState()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_get_unit_file_state (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *state)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   state));
}

/**
 * realm_dbus_systemd_manager_complete_enable_unit_files:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @carries_install_info: Parameter to return.
 * @changes: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.EnableUnitFiles">EnableUnitFiles()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_enable_unit_files (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    gboolean carries_install_info,
    GVariant *changes)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b@a(sss))",
                   carries_install_info,
                   changes));
}

/**
 * realm_dbus_systemd_manager_complete_disable_unit_files:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @changes: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.DisableUnitFiles">DisableUnitFiles()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_disable_unit_files (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *changes)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(sss))",
                   changes));
}

/**
 * realm_dbus_systemd_manager_complete_reenable_unit_files:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @carries_install_info: Parameter to return.
 * @changes: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.ReenableUnitFiles">ReenableUnitFiles()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_reenable_unit_files (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    gboolean carries_install_info,
    GVariant *changes)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b@a(sss))",
                   carries_install_info,
                   changes));
}

/**
 * realm_dbus_systemd_manager_complete_link_unit_files:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @changes: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.LinkUnitFiles">LinkUnitFiles()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_link_unit_files (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *changes)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(sss))",
                   changes));
}

/**
 * realm_dbus_systemd_manager_complete_preset_unit_files:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @carries_install_info: Parameter to return.
 * @changes: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.PresetUnitFiles">PresetUnitFiles()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_preset_unit_files (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    gboolean carries_install_info,
    GVariant *changes)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b@a(sss))",
                   carries_install_info,
                   changes));
}

/**
 * realm_dbus_systemd_manager_complete_mask_unit_files:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @changes: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.MaskUnitFiles">MaskUnitFiles()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_mask_unit_files (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *changes)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(sss))",
                   changes));
}

/**
 * realm_dbus_systemd_manager_complete_unmask_unit_files:
 * @object: A #RealmDbusSystemdManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @changes: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-systemd1-Manager.UnmaskUnitFiles">UnmaskUnitFiles()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
realm_dbus_systemd_manager_complete_unmask_unit_files (
    RealmDbusSystemdManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *changes)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(sss))",
                   changes));
}

/* ------------------------------------------------------------------------ */

/**
 * RealmDbusSystemdManagerProxy:
 *
 * The #RealmDbusSystemdManagerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * RealmDbusSystemdManagerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #RealmDbusSystemdManagerProxy.
 */

struct _RealmDbusSystemdManagerProxyPrivate
{
  GData *qdata;
};

static void realm_dbus_systemd_manager_proxy_iface_init (RealmDbusSystemdManagerIface *iface);

G_DEFINE_TYPE_WITH_CODE (RealmDbusSystemdManagerProxy, realm_dbus_systemd_manager_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (REALM_DBUS_SYSTEMD_TYPE_MANAGER, realm_dbus_systemd_manager_proxy_iface_init));

static void
realm_dbus_systemd_manager_proxy_finalize (GObject *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (realm_dbus_systemd_manager_proxy_parent_class)->finalize (object);
}

static void
realm_dbus_systemd_manager_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 32);
  info = _realm_dbus_systemd_manager_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
realm_dbus_systemd_manager_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  error = NULL;
  if (!g_dbus_proxy_call_finish (proxy, res, &error))
    {
      g_warning ("Error setting property `%s' on interface org.freedesktop.systemd1.Manager: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
}

static void
realm_dbus_systemd_manager_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 32);
  info = _realm_dbus_systemd_manager_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.freedesktop.systemd1.Manager", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) realm_dbus_systemd_manager_proxy_set_property_cb, (gpointer) info);
  g_variant_unref (variant);
}

static void
realm_dbus_systemd_manager_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_realm_dbus_systemd_manager_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], REALM_DBUS_SYSTEMD_TYPE_MANAGER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, REALM_DBUS_SYSTEMD_TYPE_MANAGER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
realm_dbus_systemd_manager_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_systemd_manager_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_systemd_manager_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_version (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Version");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_distribution (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Distribution");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_features (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Features");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_tainted (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Tainted");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_running_as (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "RunningAs");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint64 
realm_dbus_systemd_manager_proxy_get_init_rdtimestamp (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "InitRDTimestamp");
  if (variant != NULL)
    {
      value = g_variant_get_uint64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint64 
realm_dbus_systemd_manager_proxy_get_init_rdtimestamp_monotonic (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "InitRDTimestampMonotonic");
  if (variant != NULL)
    {
      value = g_variant_get_uint64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint64 
realm_dbus_systemd_manager_proxy_get_startup_timestamp (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "StartupTimestamp");
  if (variant != NULL)
    {
      value = g_variant_get_uint64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint64 
realm_dbus_systemd_manager_proxy_get_startup_timestamp_monotonic (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "StartupTimestampMonotonic");
  if (variant != NULL)
    {
      value = g_variant_get_uint64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint64 
realm_dbus_systemd_manager_proxy_get_finish_timestamp (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "FinishTimestamp");
  if (variant != NULL)
    {
      value = g_variant_get_uint64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint64 
realm_dbus_systemd_manager_proxy_get_finish_timestamp_monotonic (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "FinishTimestampMonotonic");
  if (variant != NULL)
    {
      value = g_variant_get_uint64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_log_level (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LogLevel");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_log_target (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LogTarget");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
realm_dbus_systemd_manager_proxy_get_nnames (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "NNames");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
realm_dbus_systemd_manager_proxy_get_njobs (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "NJobs");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
realm_dbus_systemd_manager_proxy_get_ninstalled_jobs (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "NInstalledJobs");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
realm_dbus_systemd_manager_proxy_get_nfailed_jobs (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "NFailedJobs");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gdouble 
realm_dbus_systemd_manager_proxy_get_progress (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  gdouble value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Progress");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_proxy_get_environment (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "Environment");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Environment");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "Environment", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
realm_dbus_systemd_manager_proxy_get_confirm_spawn (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ConfirmSpawn");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
realm_dbus_systemd_manager_proxy_get_show_status (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ShowStatus");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_proxy_get_unit_path (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "UnitPath");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "UnitPath");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "UnitPath", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_notify_socket (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "NotifySocket");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_control_group_hierarchy (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ControlGroupHierarchy");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
realm_dbus_systemd_manager_proxy_get_mount_auto (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MountAuto");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
realm_dbus_systemd_manager_proxy_get_swap_auto (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SwapAuto");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_proxy_get_default_controllers (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "DefaultControllers");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DefaultControllers");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "DefaultControllers", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_default_standard_output (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DefaultStandardOutput");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
realm_dbus_systemd_manager_proxy_get_default_standard_error (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DefaultStandardError");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
realm_dbus_systemd_manager_proxy_get_sys_vconsole (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SysVConsole");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_proxy_get_sys_vinit_path (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "SysVInitPath");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SysVInitPath");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "SysVInitPath", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_proxy_get_sys_vrcnd_path (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerProxy *proxy = REALM_DBUS_SYSTEMD_MANAGER_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "SysVRcndPath");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SysVRcndPath");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "SysVRcndPath", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static void
realm_dbus_systemd_manager_proxy_init (RealmDbusSystemdManagerProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, REALM_DBUS_SYSTEMD_TYPE_MANAGER_PROXY, RealmDbusSystemdManagerProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), realm_dbus_systemd_manager_interface_info ());
}

static void
realm_dbus_systemd_manager_proxy_class_init (RealmDbusSystemdManagerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (RealmDbusSystemdManagerProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = realm_dbus_systemd_manager_proxy_finalize;
  gobject_class->get_property = realm_dbus_systemd_manager_proxy_get_property;
  gobject_class->set_property = realm_dbus_systemd_manager_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = realm_dbus_systemd_manager_proxy_g_signal;
  proxy_class->g_properties_changed = realm_dbus_systemd_manager_proxy_g_properties_changed;


  realm_dbus_systemd_manager_override_properties (gobject_class, 1);
}

static void
realm_dbus_systemd_manager_proxy_iface_init (RealmDbusSystemdManagerIface *iface)
{
  iface->get_version = realm_dbus_systemd_manager_proxy_get_version;
  iface->get_distribution = realm_dbus_systemd_manager_proxy_get_distribution;
  iface->get_features = realm_dbus_systemd_manager_proxy_get_features;
  iface->get_tainted = realm_dbus_systemd_manager_proxy_get_tainted;
  iface->get_running_as = realm_dbus_systemd_manager_proxy_get_running_as;
  iface->get_init_rdtimestamp = realm_dbus_systemd_manager_proxy_get_init_rdtimestamp;
  iface->get_init_rdtimestamp_monotonic = realm_dbus_systemd_manager_proxy_get_init_rdtimestamp_monotonic;
  iface->get_startup_timestamp = realm_dbus_systemd_manager_proxy_get_startup_timestamp;
  iface->get_startup_timestamp_monotonic = realm_dbus_systemd_manager_proxy_get_startup_timestamp_monotonic;
  iface->get_finish_timestamp = realm_dbus_systemd_manager_proxy_get_finish_timestamp;
  iface->get_finish_timestamp_monotonic = realm_dbus_systemd_manager_proxy_get_finish_timestamp_monotonic;
  iface->get_log_level = realm_dbus_systemd_manager_proxy_get_log_level;
  iface->get_log_target = realm_dbus_systemd_manager_proxy_get_log_target;
  iface->get_nnames = realm_dbus_systemd_manager_proxy_get_nnames;
  iface->get_njobs = realm_dbus_systemd_manager_proxy_get_njobs;
  iface->get_ninstalled_jobs = realm_dbus_systemd_manager_proxy_get_ninstalled_jobs;
  iface->get_nfailed_jobs = realm_dbus_systemd_manager_proxy_get_nfailed_jobs;
  iface->get_progress = realm_dbus_systemd_manager_proxy_get_progress;
  iface->get_environment = realm_dbus_systemd_manager_proxy_get_environment;
  iface->get_confirm_spawn = realm_dbus_systemd_manager_proxy_get_confirm_spawn;
  iface->get_show_status = realm_dbus_systemd_manager_proxy_get_show_status;
  iface->get_unit_path = realm_dbus_systemd_manager_proxy_get_unit_path;
  iface->get_notify_socket = realm_dbus_systemd_manager_proxy_get_notify_socket;
  iface->get_control_group_hierarchy = realm_dbus_systemd_manager_proxy_get_control_group_hierarchy;
  iface->get_mount_auto = realm_dbus_systemd_manager_proxy_get_mount_auto;
  iface->get_swap_auto = realm_dbus_systemd_manager_proxy_get_swap_auto;
  iface->get_default_controllers = realm_dbus_systemd_manager_proxy_get_default_controllers;
  iface->get_default_standard_output = realm_dbus_systemd_manager_proxy_get_default_standard_output;
  iface->get_default_standard_error = realm_dbus_systemd_manager_proxy_get_default_standard_error;
  iface->get_sys_vconsole = realm_dbus_systemd_manager_proxy_get_sys_vconsole;
  iface->get_sys_vinit_path = realm_dbus_systemd_manager_proxy_get_sys_vinit_path;
  iface->get_sys_vrcnd_path = realm_dbus_systemd_manager_proxy_get_sys_vrcnd_path;
}

/**
 * realm_dbus_systemd_manager_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-systemd1-Manager.top_of_page">org.freedesktop.systemd1.Manager</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_proxy_new_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
realm_dbus_systemd_manager_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (REALM_DBUS_SYSTEMD_TYPE_MANAGER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.systemd1.Manager", NULL);
}

/**
 * realm_dbus_systemd_manager_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with realm_dbus_systemd_manager_proxy_new().
 *
 * Returns: (transfer full) (type RealmDbusSystemdManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusSystemdManager *
realm_dbus_systemd_manager_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return REALM_DBUS_SYSTEMD_MANAGER (ret);
  else
    return NULL;
}

/**
 * realm_dbus_systemd_manager_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-systemd1-Manager.top_of_page">org.freedesktop.systemd1.Manager</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type RealmDbusSystemdManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusSystemdManager *
realm_dbus_systemd_manager_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (REALM_DBUS_SYSTEMD_TYPE_MANAGER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.systemd1.Manager", NULL);
  if (ret != NULL)
    return REALM_DBUS_SYSTEMD_MANAGER (ret);
  else
    return NULL;
}


/**
 * realm_dbus_systemd_manager_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like realm_dbus_systemd_manager_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call realm_dbus_systemd_manager_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See realm_dbus_systemd_manager_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
realm_dbus_systemd_manager_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (REALM_DBUS_SYSTEMD_TYPE_MANAGER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.systemd1.Manager", NULL);
}

/**
 * realm_dbus_systemd_manager_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to realm_dbus_systemd_manager_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with realm_dbus_systemd_manager_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type RealmDbusSystemdManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusSystemdManager *
realm_dbus_systemd_manager_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return REALM_DBUS_SYSTEMD_MANAGER (ret);
  else
    return NULL;
}

/**
 * realm_dbus_systemd_manager_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like realm_dbus_systemd_manager_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See realm_dbus_systemd_manager_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type RealmDbusSystemdManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
RealmDbusSystemdManager *
realm_dbus_systemd_manager_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (REALM_DBUS_SYSTEMD_TYPE_MANAGER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.systemd1.Manager", NULL);
  if (ret != NULL)
    return REALM_DBUS_SYSTEMD_MANAGER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * RealmDbusSystemdManagerSkeleton:
 *
 * The #RealmDbusSystemdManagerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * RealmDbusSystemdManagerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #RealmDbusSystemdManagerSkeleton.
 */

struct _RealmDbusSystemdManagerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_realm_dbus_systemd_manager_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], REALM_DBUS_SYSTEMD_TYPE_MANAGER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, REALM_DBUS_SYSTEMD_TYPE_MANAGER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_realm_dbus_systemd_manager_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_systemd_manager_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_realm_dbus_systemd_manager_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_realm_dbus_systemd_manager_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _realm_dbus_systemd_manager_skeleton_vtable =
{
  _realm_dbus_systemd_manager_skeleton_handle_method_call,
  _realm_dbus_systemd_manager_skeleton_handle_get_property,
  _realm_dbus_systemd_manager_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
realm_dbus_systemd_manager_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return realm_dbus_systemd_manager_interface_info ();
}

static GDBusInterfaceVTable *
realm_dbus_systemd_manager_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_realm_dbus_systemd_manager_skeleton_vtable;
}

static GVariant *
realm_dbus_systemd_manager_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_realm_dbus_systemd_manager_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _realm_dbus_systemd_manager_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _realm_dbus_systemd_manager_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _realm_dbus_systemd_manager_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.systemd1.Manager", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _realm_dbus_systemd_manager_emit_changed (gpointer user_data);

static void
realm_dbus_systemd_manager_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _realm_dbus_systemd_manager_emit_changed (skeleton);
}

static void
_realm_dbus_systemd_manager_on_signal_unit_new (
    RealmDbusSystemdManager *object,
    const gchar *arg_id,
    const gchar *arg_unit)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(so)",
                   arg_id,
                   arg_unit));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.systemd1.Manager", "UnitNew",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_realm_dbus_systemd_manager_on_signal_unit_removed (
    RealmDbusSystemdManager *object,
    const gchar *arg_id,
    const gchar *arg_unit)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(so)",
                   arg_id,
                   arg_unit));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.systemd1.Manager", "UnitRemoved",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_realm_dbus_systemd_manager_on_signal_job_new (
    RealmDbusSystemdManager *object,
    guint arg_id,
    const gchar *arg_job)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(uo)",
                   arg_id,
                   arg_job));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.systemd1.Manager", "JobNew",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_realm_dbus_systemd_manager_on_signal_job_removed (
    RealmDbusSystemdManager *object,
    guint arg_id,
    const gchar *arg_job,
    const gchar *arg_result)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(uos)",
                   arg_id,
                   arg_job,
                   arg_result));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.systemd1.Manager", "JobRemoved",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_realm_dbus_systemd_manager_on_signal_startup_finished (
    RealmDbusSystemdManager *object,
    guint64 arg_kernel,
    guint64 arg_initrd,
    guint64 arg_userspace,
    guint64 arg_total)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(tttt)",
                   arg_kernel,
                   arg_initrd,
                   arg_userspace,
                   arg_total));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.systemd1.Manager", "StartupFinished",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_realm_dbus_systemd_manager_on_signal_unit_files_changed (
    RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.systemd1.Manager", "UnitFilesChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void realm_dbus_systemd_manager_skeleton_iface_init (RealmDbusSystemdManagerIface *iface);
G_DEFINE_TYPE_WITH_CODE (RealmDbusSystemdManagerSkeleton, realm_dbus_systemd_manager_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (REALM_DBUS_SYSTEMD_TYPE_MANAGER, realm_dbus_systemd_manager_skeleton_iface_init));

static void
realm_dbus_systemd_manager_skeleton_finalize (GObject *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint n;
  for (n = 0; n < 32; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (realm_dbus_systemd_manager_skeleton_parent_class)->finalize (object);
}

static void
realm_dbus_systemd_manager_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 32);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_realm_dbus_systemd_manager_emit_changed (gpointer user_data)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *l;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.freedesktop.systemd1.Manager",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (l = connections; l != NULL; l = l->next)
        {
          GDBusConnection *connection = l->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_realm_dbus_systemd_manager_schedule_emit_changed (RealmDbusSystemdManagerSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
realm_dbus_systemd_manager_skeleton_notify (GObject      *object,
  GParamSpec *pspec)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _realm_dbus_systemd_manager_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
realm_dbus_systemd_manager_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 32);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _realm_dbus_systemd_manager_schedule_emit_changed (skeleton, _realm_dbus_systemd_manager_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
realm_dbus_systemd_manager_skeleton_init (RealmDbusSystemdManagerSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, REALM_DBUS_SYSTEMD_TYPE_MANAGER_SKELETON, RealmDbusSystemdManagerSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 32);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_UINT64);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_UINT64);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_UINT64);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_UINT64);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_UINT64);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_UINT64);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[12], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[13], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[14], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[15], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[16], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[17], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[18], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[19], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[20], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[21], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[22], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[23], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[24], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[25], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[26], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[27], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[28], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[29], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[30], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[31], G_TYPE_STRV);
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_version (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_distribution (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_features (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_tainted (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_running_as (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint64 
realm_dbus_systemd_manager_skeleton_get_init_rdtimestamp (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint64 (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint64 
realm_dbus_systemd_manager_skeleton_get_init_rdtimestamp_monotonic (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint64 (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint64 
realm_dbus_systemd_manager_skeleton_get_startup_timestamp (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint64 (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint64 
realm_dbus_systemd_manager_skeleton_get_startup_timestamp_monotonic (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint64 (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint64 
realm_dbus_systemd_manager_skeleton_get_finish_timestamp (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint64 (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint64 
realm_dbus_systemd_manager_skeleton_get_finish_timestamp_monotonic (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint64 (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_log_level (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_log_target (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[12]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
realm_dbus_systemd_manager_skeleton_get_nnames (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[13]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
realm_dbus_systemd_manager_skeleton_get_njobs (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[14]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
realm_dbus_systemd_manager_skeleton_get_ninstalled_jobs (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[15]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
realm_dbus_systemd_manager_skeleton_get_nfailed_jobs (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[16]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble 
realm_dbus_systemd_manager_skeleton_get_progress (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_double (&(skeleton->priv->properties[17]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_skeleton_get_environment (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[18]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
realm_dbus_systemd_manager_skeleton_get_confirm_spawn (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[19]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
realm_dbus_systemd_manager_skeleton_get_show_status (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[20]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_skeleton_get_unit_path (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[21]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_notify_socket (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[22]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_control_group_hierarchy (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[23]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
realm_dbus_systemd_manager_skeleton_get_mount_auto (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[24]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
realm_dbus_systemd_manager_skeleton_get_swap_auto (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[25]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_skeleton_get_default_controllers (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[26]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_default_standard_output (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[27]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
realm_dbus_systemd_manager_skeleton_get_default_standard_error (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[28]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
realm_dbus_systemd_manager_skeleton_get_sys_vconsole (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[29]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_skeleton_get_sys_vinit_path (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[30]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
realm_dbus_systemd_manager_skeleton_get_sys_vrcnd_path (RealmDbusSystemdManager *object)
{
  RealmDbusSystemdManagerSkeleton *skeleton = REALM_DBUS_SYSTEMD_MANAGER_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[31]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
realm_dbus_systemd_manager_skeleton_class_init (RealmDbusSystemdManagerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (RealmDbusSystemdManagerSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = realm_dbus_systemd_manager_skeleton_finalize;
  gobject_class->get_property = realm_dbus_systemd_manager_skeleton_get_property;
  gobject_class->set_property = realm_dbus_systemd_manager_skeleton_set_property;
  gobject_class->notify       = realm_dbus_systemd_manager_skeleton_notify;


  realm_dbus_systemd_manager_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = realm_dbus_systemd_manager_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = realm_dbus_systemd_manager_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = realm_dbus_systemd_manager_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = realm_dbus_systemd_manager_skeleton_dbus_interface_get_vtable;
}

static void
realm_dbus_systemd_manager_skeleton_iface_init (RealmDbusSystemdManagerIface *iface)
{
  iface->unit_new = _realm_dbus_systemd_manager_on_signal_unit_new;
  iface->unit_removed = _realm_dbus_systemd_manager_on_signal_unit_removed;
  iface->job_new = _realm_dbus_systemd_manager_on_signal_job_new;
  iface->job_removed = _realm_dbus_systemd_manager_on_signal_job_removed;
  iface->startup_finished = _realm_dbus_systemd_manager_on_signal_startup_finished;
  iface->unit_files_changed = _realm_dbus_systemd_manager_on_signal_unit_files_changed;
  iface->get_version = realm_dbus_systemd_manager_skeleton_get_version;
  iface->get_distribution = realm_dbus_systemd_manager_skeleton_get_distribution;
  iface->get_features = realm_dbus_systemd_manager_skeleton_get_features;
  iface->get_tainted = realm_dbus_systemd_manager_skeleton_get_tainted;
  iface->get_running_as = realm_dbus_systemd_manager_skeleton_get_running_as;
  iface->get_init_rdtimestamp = realm_dbus_systemd_manager_skeleton_get_init_rdtimestamp;
  iface->get_init_rdtimestamp_monotonic = realm_dbus_systemd_manager_skeleton_get_init_rdtimestamp_monotonic;
  iface->get_startup_timestamp = realm_dbus_systemd_manager_skeleton_get_startup_timestamp;
  iface->get_startup_timestamp_monotonic = realm_dbus_systemd_manager_skeleton_get_startup_timestamp_monotonic;
  iface->get_finish_timestamp = realm_dbus_systemd_manager_skeleton_get_finish_timestamp;
  iface->get_finish_timestamp_monotonic = realm_dbus_systemd_manager_skeleton_get_finish_timestamp_monotonic;
  iface->get_log_level = realm_dbus_systemd_manager_skeleton_get_log_level;
  iface->get_log_target = realm_dbus_systemd_manager_skeleton_get_log_target;
  iface->get_nnames = realm_dbus_systemd_manager_skeleton_get_nnames;
  iface->get_njobs = realm_dbus_systemd_manager_skeleton_get_njobs;
  iface->get_ninstalled_jobs = realm_dbus_systemd_manager_skeleton_get_ninstalled_jobs;
  iface->get_nfailed_jobs = realm_dbus_systemd_manager_skeleton_get_nfailed_jobs;
  iface->get_progress = realm_dbus_systemd_manager_skeleton_get_progress;
  iface->get_environment = realm_dbus_systemd_manager_skeleton_get_environment;
  iface->get_confirm_spawn = realm_dbus_systemd_manager_skeleton_get_confirm_spawn;
  iface->get_show_status = realm_dbus_systemd_manager_skeleton_get_show_status;
  iface->get_unit_path = realm_dbus_systemd_manager_skeleton_get_unit_path;
  iface->get_notify_socket = realm_dbus_systemd_manager_skeleton_get_notify_socket;
  iface->get_control_group_hierarchy = realm_dbus_systemd_manager_skeleton_get_control_group_hierarchy;
  iface->get_mount_auto = realm_dbus_systemd_manager_skeleton_get_mount_auto;
  iface->get_swap_auto = realm_dbus_systemd_manager_skeleton_get_swap_auto;
  iface->get_default_controllers = realm_dbus_systemd_manager_skeleton_get_default_controllers;
  iface->get_default_standard_output = realm_dbus_systemd_manager_skeleton_get_default_standard_output;
  iface->get_default_standard_error = realm_dbus_systemd_manager_skeleton_get_default_standard_error;
  iface->get_sys_vconsole = realm_dbus_systemd_manager_skeleton_get_sys_vconsole;
  iface->get_sys_vinit_path = realm_dbus_systemd_manager_skeleton_get_sys_vinit_path;
  iface->get_sys_vrcnd_path = realm_dbus_systemd_manager_skeleton_get_sys_vrcnd_path;
}

/**
 * realm_dbus_systemd_manager_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-systemd1-Manager.top_of_page">org.freedesktop.systemd1.Manager</link>.
 *
 * Returns: (transfer full) (type RealmDbusSystemdManagerSkeleton): The skeleton object.
 */
RealmDbusSystemdManager *
realm_dbus_systemd_manager_skeleton_new (void)
{
  return REALM_DBUS_SYSTEMD_MANAGER (g_object_new (REALM_DBUS_SYSTEMD_TYPE_MANAGER_SKELETON, NULL));
}

