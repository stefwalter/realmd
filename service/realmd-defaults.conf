# Default values for realmd

[paths]
net = /usr/bin/net
winbindd = /usr/sbin/winbindd
smb.conf = /etc/smb.conf

[services]
winbind = winbind

[samba-packages]

[sssd-ad-packages]

[commands]

[user]
shell = /bin/bash